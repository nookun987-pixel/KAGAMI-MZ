# CHARACTER_CANON_V2_LORA_SUBSTRATE_PATCH_PROPOSAL_V0_1

Generated by: `CHARACTER_LANE_AUTONOMOUS_RUNNER_V0_1.md` (run #17)
Generated on: 2026-05-29
Mode: PROPOSAL-ONLY. This file describes the canon V2 changes that would be applied if operator approves. It does NOT edit `MIKAGE_ZENITH_CANON_V2.md`. The canon V2 file mtime stays unchanged at this pass.

---

## 1. OPERATOR DIRECTIVE

```text
Q_NEXT = CANON
(picked in CHARACTER_REGISTRY_REWRITE_2026-05-29_F_REPORT.md §9)
```

Trigger reference: `CHARACTER_LORA_FACTION_LOCK_2026-05-29_F_REPORT.md` §8 created the pending canon update task `LORA_SUBSTRATE_§7_PATCH_PROPOSAL`. This file is that proposal.

Approval scope (per runner spec §5 + `CLAUDE.md` "Do not approve canon"): runner is authorized to write THIS proposal file + update its own pointer. Runner is NOT authorized to edit `MIKAGE_ZENITH_CANON_V2.md`. Even with a future `OPERATOR_APPROVED = YES — TARGET = MIKAGE_ZENITH_CANON_V2.md` token, runner will draft a final canon-edit diff as a SEPARATE file for operator review; runner will not auto-apply the edit.

---

## 2. WHY THIS PATCH IS NEEDED

After Lock 4F (Run #7), `LORA_FACTION = META_SUBSTRATE` is canonical-direction, but:

1. **Canon V2 §7 has no §7.0** — the locked Three Ideologies start at §7.1. There is no canonical place for "substrate beneath all three" today.
2. **Canon V2 §8 entity list has no LORA entry** — entities 8.1–8.5 cover Mikage, Lyre, ARCHON-IX & LYRA-0, Dr. Aris, Tai Vane. LORA is absent.
3. **Canon V2 §15 ENTITIES directory layout has no `lora.json`** — only 4 JSONs are listed (mikage_zenith / commander_lyre / zenith_blade / unbreakable_shield).
4. **Cross-section drift** — §8.2 Lyre says "Allegiance: Empire" but Lyre actually serves LORA per locks; §11 micro-moments don't include any LORA-specific event (the Golden Patch).

This proposal closes all four gaps.

---

## 3. TARGET PATCH — TWO INTEGRATION LEVELS (operator picks)

### 3.A — MINIMAL PATCH (Level A)

Adds §7.0 only. Leaves §8 entity list and §15 ENTITIES tree unchanged. Smallest canon-touching footprint.

**Diff vs current `MIKAGE_ZENITH_CANON_V2.md`:**

Insert BEFORE existing line `### 7.1 THREE IDEOLOGIES` (current line 343):

```markdown
### 7.0 SUBSTRATE LAYER — META — LOCKED 2026-05-29

**LORA (Root Architect / System Substrate):**
- Meta-level operating condition; all three ideologies execute on top of LORA's substrate
- Non-physical system presence; no humanoid form (Lock 3A 2026-05-29)
- White Void + Cold Cyan server-sync + Clean Digital Gold (Golden Patch event)
- Refactor through absolute system law; chaos treated as corrupted code
- Does not take sides; ideologies serve the substrate
- Source: CHARACTER_LORA_FORM_LOCK_2026-05-29_A_REPORT.md + CHARACTER_LORA_FACTION_LOCK_2026-05-29_F_REPORT.md

```

Total addition: ~10 lines. Zero changes to §7.1 / §7.2 / §7.3 / §8 / §15. The Three Ideologies remain LOCKED as before, now sitting on top of the new substrate layer.

### 3.B — FULL INTEGRATION (Level B)

Adds §7.0 + §8.6 (LORA entity) + §15 (lora.json) + cross-reference note in §8.2 (Lyre serves LORA) + new §11 micro-moment (Golden Patch). Larger footprint, closes all four gaps.

**Diff vs current `MIKAGE_ZENITH_CANON_V2.md`:**

**Change 1**: Insert BEFORE `### 7.1 THREE IDEOLOGIES`:

```markdown
### 7.0 SUBSTRATE LAYER — META — LOCKED 2026-05-29

**LORA (Root Architect / System Substrate):**
- Meta-level operating condition; all three ideologies execute on top of LORA's substrate
- Non-physical system presence; no humanoid form (Lock 3A 2026-05-29)
- White Void + Cold Cyan server-sync + Clean Digital Gold (Golden Patch event)
- Refactor through absolute system law; chaos treated as corrupted code
- Does not take sides; ideologies serve the substrate
- Source: CHARACTER_LORA_FORM_LOCK_2026-05-29_A_REPORT.md + CHARACTER_LORA_FACTION_LOCK_2026-05-29_F_REPORT.md
```

**Change 2**: Append after current §8.5 (Tai Vane) block (current line ~429):

```markdown
### 8.6 LORA

**Status:** Substrate / System Presence
**Form:** Non-humanoid system entity (Lock 3A — permanent)
**Allegiance:** META_SUBSTRATE (Lock 4F — beneath all three ideologies)
**Function:** Refactor / Clean Code enforcement / reality architecture
**Visual motifs:** Mechanical Enso ring, Golden Patch event, White Void field, Cold Cyan server-sync
**Scale:** Architectural (no measurable height)
**Damage system:** N/A — does not take damage; LORA IS the system
**Relation to Mikage:** Control / dependency / ownership / refactor
**Relation to Commander Lyre:** Reality architecture and source-code control context
**Voice/dialogue profile:** CHUA_XAC_NHAN
**Visual assets:** none confirmed (Decision 2 path: no PNG)
**Source:** character_workflow/LORA_VISUAL_BRIEF_V0_1.md + LORA_VISUAL_SPEC_CLEAN_V0_1.md + LORA_ENTITY_RECORD_V0_1.md + Locks 3A + 4F
```

**Change 3**: In §8.2 COMMANDER LYRE, append one line after `**Allegiance:** Empire`:

```markdown
**Substrate alignment:** Empire executes on LORA's substrate (per §7.0); Lyre's enforcement serves Empire which serves LORA.
```

**Change 4**: In §11 MICRO-MOMENTS list, append after current item 9 (Trojan memory):

```markdown
10. **Golden Patch event:** Clean-code intervention by LORA replaces corrupted infrastructure; appears as a precise white-void inversion with cold cyan and clean digital gold accents.
```

**Change 5**: In §15 directory layout block, insert one line in the ENTITIES tree after `commander_lyre.json`:

```text
│   ├── lora.json
```

Total addition: ~28 lines across 5 insertion points. No deletions. No edits to existing content.

---

## 4. WHAT EACH LEVEL UNBLOCKS

| Unblock | Level A | Level B |
|---|---|---|
| `proposals/lora.json` has a canonical destination | ✓ (via §7.0 reference) | ✓ (via §7.0 + §8.6 + §15) |
| Promotion plan §3 step 2 (CANON precondition) | ✓ | ✓ |
| LORA appears in §8 entity list | ✗ | ✓ |
| LORA appears in §15 directory layout | ✗ | ✓ |
| Lyre's "serves LORA" relationship is canon-stated | ✗ | ✓ |
| Golden Patch event is canon-locked as a micro-moment | ✗ | ✓ |
| Future readers of canon V2 see LORA without reading lock reports | partial (§7.0 only) | full |
| Risk surface (lines added) | smallest (10) | moderate (28) |
| Reversibility | easiest | still easy (5 isolated insertions, no deletions) |

---

## 5. APPROVAL TOKEN BUNDLE (required to execute)

Runner spec §5 + `CLAUDE.md` "Do not approve canon" require:

For Level A:
```text
CANON_PATCH_EXECUTE       = LEVEL_A
CANON_PATCH_APPROVAL      = OPERATOR_APPROVED = YES — TARGET = MIKAGE_ZENITH_CANON_V2.md
CANON_PATCH_AUDIT_TRAIL   = REQUIRED (runner will produce a separate diff file before applying)
CANON_PATCH_SUPERSEDE_LOCKED_HEADER = NO (the §7.0 addition does NOT touch §7.1 LOCKED)
```

For Level B:
```text
CANON_PATCH_EXECUTE       = LEVEL_B
CANON_PATCH_APPROVAL      = OPERATOR_APPROVED = YES — TARGET = MIKAGE_ZENITH_CANON_V2.md
CANON_PATCH_AUDIT_TRAIL   = REQUIRED
CANON_PATCH_SUPERSEDE_LOCKED_HEADER = NO (additions only; no deletions)
CANON_PATCH_ACCEPT_§8_LIST_EXPANSION = YES (adds §8.6 LORA — new entity entry)
CANON_PATCH_ACCEPT_§15_TREE_UPDATE = YES (adds lora.json line)
CANON_PATCH_ACCEPT_§11_NEW_MICRO_MOMENT = YES (Golden Patch as item 10)
CANON_PATCH_ACCEPT_§8.2_CROSS_REF = YES (Lyre/Empire serves LORA substrate)
```

Even with all tokens issued, runner will FIRST produce a final diff file `CHARACTER_CANON_V2_LORA_SUBSTRATE_PATCH_DIFF_V0_1.md` and request operator review before any canon V2 byte changes. The runner does NOT auto-apply.

Without these tokens, runner refuses to edit canon V2 and emits a `BLOCKED_BY_OPERATOR_DECISION` report.

---

## 6. EXECUTION SEQUENCE (when approved)

This is the step list runner would follow upon receiving the token bundle. **Not executed at this pass.**

1. **Pre-flight** (read-only): Re-read `MIKAGE_ZENITH_CANON_V2.md`. Confirm §7.1 starts at line ~343 (within ±5 lines of current count). Confirm §15 ENTITIES tree intact.
2. **Diff file**: Write `CHARACTER_CANON_V2_LORA_SUBSTRATE_PATCH_DIFF_V0_1.md` showing the exact byte-level diff. Halt and request operator review.
3. **Operator review confirmation** (required before next step): Operator answers `CANON_PATCH_DIFF_APPROVED = YES` in the diff file's answer block.
4. **Apply patch**: Insert the additions per Level A or Level B. Use Edit tool with surgical insertions; no overwrites.
5. **Post-flight** (read-only): Re-read canon V2 to confirm:
   - New section header(s) present at expected positions.
   - No accidental deletions in adjacent text.
   - `Last Verified` line at §16 still reads `2026-03-19` UNLESS operator also approved updating it.
6. **Update `Last Updated` and `Last Verified` at §16** — only if operator separately approves `CANON_PATCH_BUMP_LAST_VERIFIED = YES`. Default: leave §16 unchanged (this is a doctrinal change, not a re-verification).
7. **Update V0.2 registry**: Add an entry to `CHARACTER_PACKAGE_REGISTRY_V0_2_2026-05-29.md` §5 noting that LORA_SUBSTRATE_§7_PATCH_PROPOSAL is now APPLIED.
8. **Update runner pointer**: Mark `CANON_V2_UPDATE_TASKS = LORA_SUBSTRATE_§7_PATCH (APPLIED)` + remove from pending.
9. **Optional commit** (requires `COMMIT <message>` + `PUSH = YES` tokens): runner stages and commits. Push requires explicit token.

---

## 7. ROLLBACK PLAN

| Failure point | Rollback |
|---|---|
| Pre-flight detects unexpected canon V2 content | Halt; emit BLOCKED report; no changes |
| Diff file step | Diff file deleted; no canon V2 changes |
| Operator review rejects diff | Diff file annotated with rejection; no canon V2 changes |
| Apply patch partial | `git checkout MIKAGE_ZENITH_CANON_V2.md` (un-staged) OR `git revert <commit>` |
| Post-flight detects accidental deletion | Same: revert canon V2; investigate |
| Commit completed but downstream broke | Coordinate with anyone consuming canon V2 to roll forward or revert |

Key invariant: this proposal file (`CHARACTER_CANON_V2_LORA_SUBSTRATE_PATCH_PROPOSAL_V0_1.md`) is never deleted during rollback. It remains the audit trail.

---

## 8. SIDE EFFECTS LIST

Beyond the line-level insertions, the patch causes the following knock-on effects:

| Effect | Level A | Level B |
|---|---|---|
| `proposals/lora.json` `$canon_v2_alignment` field becomes "RESOLVED — §7.0" instead of "PENDING" | ✓ | ✓ |
| `proposals/commander_lyre.json` `allegiance.substrate_alignment` field could be added (text-only) reflecting §8.2 cross-ref | ✗ | recommended |
| Promotion plan §3 precondition P_CANON_PATCH satisfied | ✓ | ✓ |
| Future entity readers (validator, runner, Codex) see LORA in canon V2 search | only via §7.0 | yes, fully indexed |
| Reference Sheet V1 CANON_LOCKED status (Mikage) unaffected | ✓ | ✓ |
| Workspace ComfyUI validator (mikage_zenith.json profile) needs update? | NO — LORA is not a Mikage validation target | NO same |
| `MIKAGE_PUBLIC_REVEAL_CANDIDATE_BOARD_V0_1.md` §7 "LORA — NO VISUAL ASSETS" remains accurate | ✓ | ✓ |
| Design system "two-layer" disclaimer (brand vs film) unaffected | ✓ | ✓ |

---

## 9. WHAT THIS PROPOSAL DOES NOT TOUCH AT THIS PASS

- `MIKAGE_ZENITH_CANON_V2.md` — **unchanged at this pass** (verify mtime stays 2026-04-28).
- 5 proposals/ JSONs — unchanged.
- 3 lock reports + V0.1 registry + V0.2 registry + boards + draft reports + promotion plan + design system files — unchanged.
- Mikage Bible, Lyre brief+spec, LORA brief+spec+entity record, lineup, Reference Sheet (incl. CANON_LOCK ZIP) — unchanged.
- May 23 handoff (AUDIO + AUDIO copy) — unchanged.
- 3 AUDIO ZIPs — unchanged (NOT opened).
- Workspace ComfyUI mikage_zenith.json — unchanged.
- `CLAUDE.md` — unchanged.
- No ZIP opened. No PNG opened. No render. No commit. No move. No copy. No JSON status change. No canon byte changed.

---

## 10. ANSWER BLOCK

Fill in ONE of the following and save the file. Runner reads on next invocation.

```
CANON_PATCH_OPTION = 
CANON_PATCH_APPROVAL = 
```

Examples:
- Minimal patch with approval: `CANON_PATCH_OPTION = LEVEL_A` AND `CANON_PATCH_APPROVAL = OPERATOR_APPROVED = YES — TARGET = MIKAGE_ZENITH_CANON_V2.md`
- Full integration with approval: `CANON_PATCH_OPTION = LEVEL_B` AND `CANON_PATCH_APPROVAL = OPERATOR_APPROVED = YES — TARGET = MIKAGE_ZENITH_CANON_V2.md`
- Defer (proposal stays as draft, no canon change): `CANON_PATCH_OPTION = DEFER` AND `CANON_PATCH_APPROVAL = OPERATOR_APPROVED = NO — DEFER`
- Reject (operator wants a different patch): `CANON_PATCH_OPTION = REJECT` AND `CANON_PATCH_APPROVAL = OPERATOR_APPROVED = NO — REWRITE_REQUESTED`

If `LEVEL_A` or `LEVEL_B` chosen with approval: runner produces `CHARACTER_CANON_V2_LORA_SUBSTRATE_PATCH_DIFF_V0_1.md` (diff file) and HALTS for operator final review. Runner does NOT auto-apply canon edit even with approval — the diff file is the second-gate.

If `DEFER`: runner records the deferral; proposal stays on disk; no canon change.

If `REJECT`: runner emits a follow-up board asking what direction operator wants instead.

---

## 11. REMAINING UNRESOLVED DECISIONS

| ID | Decision | Status |
|---|---|---|
| 2 | Lyre PNG candidate status | UNRESOLVED |
| 5 | THE ROOT ARCHITECT name collision | UNRESOLVED |

D6 = SOFT-RESOLVED (Registry V0.2 written).
D7 = SOFT-RESOLVED (5 proposals/ drafts).
D1 = LOCKED (Lyre 1A).
D3 = LOCKED (LORA form 3A).
D4 = LOCKED (LORA faction 4F).

Pending text-only tasks: PROMOTE execution (full token bundle), DRIFT_FIX (5 items), VERIFY_CAL, BACKFILL_ENTITY_KIND, MORE_ENTITIES.

---

## 12. NEXT SAFE TASK

```
Q_NEXT = LEVEL_A          → choose minimal canon patch (with approval token, produces diff file next)
Q_NEXT = LEVEL_B          → choose full canon patch (with approval token, produces diff file next)
Q_NEXT = DEFER            → keep proposal as draft; no canon change
Q_NEXT = REJECT           → reject proposal; runner asks for new direction
Q_NEXT = 2                → focus board for Lyre PNG (parallel track)
Q_NEXT = 5                → focus board for THE ROOT ARCHITECT collision (parallel track)
Q_NEXT = DRIFT_FIX        → focus board on 5 deferred drift items
Q_NEXT = MORE_ENTITIES    → draft ARCHON-IX/LYRA-0/Dr. Aris/Tai Vane JSONs
Q_NEXT = STOP             → runner stops
```

Answer in this file's §10 answer block OR via a new directive in the next message.

Runner suggestion (deterministic): **`LEVEL_A`** if minimizing canon footprint matters most; **`LEVEL_B`** if completing the LORA integration in one canon update matters more than line-count.

— END OF CANON V2 LORA SUBSTRATE PATCH PROPOSAL V0.1 —
