# CHARACTER_OPERATOR_DECISION_BOARD_V0_1

Generated by: `CHARACTER_LANE_AUTONOMOUS_RUNNER_V0_1.md` (run #1)
Generated on: 2026-05-29
Mode: READ-ONLY. This board does NOT edit any source file. It only lists what the operator must answer before the runner can do any text-only follow-up.

Source reports synthesized:
- `character_workflow/CHARACTER_BRIEF_FIELD_GAP_V0_1.md`
- `character_workflow/CHARACTER_LYRE_LORA_IMAGE_CROSS_REPO_GREP_V0_1.md`
- `character_workflow/CHARACTER_PACKAGE_REGISTRY_2026-05-23.md`
- `character_workflow/COMMANDER_LYRE_CHARACTER_BRIEF_V0_1.md`
- `character_workflow/COMMANDER_LYRE_VISUAL_SPEC_CLEAN_V0_1.md`
- `character_workflow/LORA_VISUAL_BRIEF_V0_1.md`
- `character_workflow/LORA_VISUAL_SPEC_CLEAN_V0_1.md`
- `MIKAGE_ZENITH_CANON_V2.md`
- `CLAUDE.md`

How to answer: per item, write either `OPERATOR_APPROVED = YES — OPTION = <chosen-option-id>` or `OPERATOR_APPROVED = NO — DEFER` directly under the decision item, then save the file. The runner will pick up the answers on its next invocation. The runner will NOT execute any mutation just because an option is chosen — every mutation also needs the corresponding token from runner spec §5.

---

## DECISION 1 — LYRE VISUAL DIRECTION (DRIFT RESOLUTION)

**Question**: Which Lyre visual direction is canonical going forward?

**Sources**:
- `COMMANDER_LYRE_VISUAL_SPEC_CLEAN_V0_1.md` (status `SPEC PATCHED 2026-05-28`): porcelain white #FAFAFA + cold cyan #00FFFF + dark nickel-gray under-suit + Molecular Monowire / Force-field Lyre.
- `D:\MIKAGE ZENITH AUDIO\05. SINGULAR HEART\Downloads\MIKAGE_CHARACTER_WORKFLOW_HANDOFF_2026-05-23.md` (line 341, 2026-05-23): dark graphite / black technical armor + controlled electric violet accents + cold white edge highlights.

**Options**:
- `1A` — LOCK May 28 spec (porcelain white + cyan). Archive May 23 handoff with note `SUPERSEDED_2026-05-28`.
- `1B` — LOCK May 23 handoff (graphite + violet). Revert brief + spec to that direction.
- `1C` — REJECT both. Operator will provide a third direction.
- `1D` — DEFER. No render, no asset work on Lyre until a future decision.

**Runner recommendation (default if no answer)**: `1D` (most conservative — current state).

**If `1A` chosen**: runner must mark May 23 doc as `SUPERSEDED` (text-only annotation in a new follow-up report, NOT inside the May 23 file). Runner does NOT delete or move the May 23 file.

**If `1B` chosen**: runner cannot revert brief/spec without `OPERATOR_APPROVED = YES` on each file path explicitly listed.

**Blocker impact**: Decision 7 (`commander_lyre.json`) cannot be answered until this is resolved.

`OPERATOR_APPROVED = ` _(leave blank or fill)_
`OPTION = ` _(blank or 1A/1B/1C/1D)_

---

## DECISION 2 — LYRE PNG CANDIDATE STATUS

**Question**: Is `D:\MIKAGE ZENITH AUDIO\00. COMPANNY LIMITED\02. COMMANDER LYRE\ChatGPT Image 21_16_17 27 thg 4, 2026.png` (2,262,369 B, 2026-04-27) a Lyre character concept visual?

**Sources**:
- Grep report §3 row 1 — folder name "02. COMMANDER LYRE" + lyric file `Title IMPERIAL SHIELD.txt` in same folder calls out "Commander Lyre" by name. But folder grand-parent "00. COMPANNY LIMITED" also contains business PDFs → cấu trúc folder không thuần character workflow.
- `MIKAGE_PUBLIC_REVEAL_CANDIDATE_BOARD_V0_1.md` §6: "COMMANDER LYRE — NO VISUAL ASSETS" → board được tạo gần đây hơn PNG, đã explicit không công nhận.

**Options**:
- `2A` — Mark as `LYRE_DIRECTIONAL_CONCEPT_2026-04-27_PROVISIONAL` (not asset-locked, not public-ready). Operator confirms file is Lyre by manually opening it. Runner does NOT open or copy.
- `2B` — Mark as `NOT_LYRE` after operator inspection. Runner ignores it in all future audits.
- `2C` — `CHUA_XAC_NHAN` (status quo). No further action.

**Runner recommendation**: `2C` until operator opens PNG manually.

**Blocker impact**: Decision 1 may be informed by what this PNG actually depicts (could match May 23 graphite/violet, or May 28 porcelain/cyan, or neither).

`OPERATOR_APPROVED = `
`OPTION = `

---

## DECISION 3 — LORA ANTHROPOMORPHIC FORM

**Question**: Does LORA have a humanoid avatar form, or remain pure system-presence?

**Sources**:
- `LORA_ENTITY_RECORD_V0_1.md`: `ANTHROPOMORPHIC_FORM = CHUA_XAC_NHAN`, `LORA_BODY_STATUS = KHONG_XAC_DINH_SINH_HOC / SYSTEM_SERVER`.
- `LORA_VISUAL_BRIEF_V0_1.md` §4: `LORA_FORM_DIRECTION = SYSTEM_PRESENCE_FIRST`, "Unless a future canon source explicitly confirms a humanoid avatar."
- `LORA_VISUAL_SPEC_CLEAN_V0_1.md` §3: "LORA has no confirmed humanoid form."
- `CHARACTER_PACKAGE_REGISTRY_2026-05-23.md` §LORA rule: "Do not draw LORA as a normal humanoid height silhouette unless later canon confirms an anthropomorphic avatar."

**Options**:
- `3A` — LOCK `SYSTEM_PRESENCE_ONLY` (no humanoid avatar, ever). Treat LORA as architectural-scale system entity in all visuals.
- `3B` — LOCK `SYSTEM_PRESENCE_PLUS_OPTIONAL_AVATAR` (system-presence primary; humanoid avatar allowed only for specific scene type to be defined by operator).
- `3C` — DEFER. Keep `CHUA_XAC_NHAN`.

**Runner recommendation**: `3A` (matches all 4 source docs and is the most conservative).

**Blocker impact**: LORA `KEY_VISUAL_01` prompt seed (LORA_VISUAL_SPEC_CLEAN_V0_1.md §10) is environment-first; it works under 3A or 3C but fails interpretation under 3B without further spec.

`OPERATOR_APPROVED = `
`OPTION = `

---

## DECISION 4 — LORA FACTION

**Question**: Which faction is LORA aligned with?

**Sources**:
- `LORA_VISUAL_BRIEF_V0_1.md` §1: `Allegiance: CHUA_XAC_NHAN — not assigned to Empire or Third Axis in any confirmed source`.
- `LORA_VISUAL_SPEC_CLEAN_V0_1.md` §1: `FACTION = CHUA_XAC_NHAN — not assigned to Empire or Third Axis in any source`.
- `LORA_VISUAL_BRIEF_V0_1.md` §3 narrative table: LORA = "system above Lyre, architecture beneath Mikage" → ngụ ý META-LEVEL above both factions, but không explicit.
- Canon V2 không có entry cho LORA (theo `LORA_VISUAL_BRIEF_V0_1.md` §2).

**Options**:
- `4A` — `META_FACTION_ABOVE_BOTH` (above Empire and Third Axis; LORA is the system both factions exist inside).
- `4B` — `EMPIRE_ALIGNED` (LORA serves the White Monolith / Empire infrastructure).
- `4C` — `THIRD_AXIS_ALIGNED`.
- `4D` — `INDEPENDENT` (neither Empire nor Third Axis; not above either).
- `4E` — DEFER.

**Runner recommendation**: `4A` (matches existing narrative language in brief §3 and spec §8 — "LORA is not the antagonist. LORA is the system the antagonist serves.").

**Blocker impact**: LORA cannot be added to Canon V2 (Decision 7 area) until faction is decided.

`OPERATOR_APPROVED = `
`OPTION = `

---

## DECISION 5 — THE ROOT ARCHITECT NAME COLLISION

**Question**: Is `D:\MIKAGE ZENITH AUDIO\07. THE ROOT ARCHITECT\` (track 07 release) intentionally named after LORA's role "Root Architect / System Presence"?

**Sources**:
- LORA brief + spec define `PUBLIC_ROLE = Root Architect / System Presence`.
- Track 07 has full MV pilot workflow: cover card, 12 keyframe prompts, MP4 prototype, contact sheet, audit, GPT keyframe candidates, claude keyframe pack.
- Grep report §9: pass này không đọc lyric track 07 (`07. THE ROOT ARCHITECT/THE_ROOT_ARCHITECT_CLEAN_LYRIC_TOOLOST.txt` tồn tại nhưng ngoài scope).

**Options**:
- `5A` — INTENTIONAL. Track 07 IS LORA's track. Cover card + MV keyframes count as LORA-adjacent visual material. Future LORA audits should cross-reference track 07.
- `5B` — COINCIDENTAL. Track 07 title and LORA role share the words "Root Architect" by coincidence; no cross-reference.
- `5C` — DEFER. Operator will read lyric of track 07 and decide.

**Runner recommendation**: `5C` (cannot decide without reading lyric content; runner spec rule 8 forbids cross-link until confirmed).

**Blocker impact**:
- If `5A`: a LORA visual audit becomes possible using existing track 07 keyframe candidates (still no render, still no asset lock).
- If `5B`: rule 8 stays in force; runner ignores track 07 in LORA scope forever.
- Runner cannot read lyric file proactively (would be text-only and is ALLOWED under read-only, but runner spec §3.A priority 1 only triggers if grep is > 7 days old).

`OPERATOR_APPROVED = `
`OPTION = `

---

## DECISION 6 — REGISTRY ZIP MISMATCH

**Question**: How to resolve the 5 ZIP packages registered in `CHARACTER_PACKAGE_REGISTRY_2026-05-23.md` that do NOT exist on any of the 8 mounted folders?

**Sources**:
- Grep report §5.1 + §6 + §7: 5/5 registered ZIPs missing; folder `character_workflow/packages/` exists but empty.
- 2 unregistered ZIPs in AUDIO/05. SINGULAR HEART/Downloads have similar names: `MIKAGE_COMMANDER_LYRE_DIRECTION_BRIEF_V0_1_PACKAGE.zip` (89,224 B), `MIKAGE_LORA_INTERFACE_BOARD_V0_1_PACKAGE.zip` (108,991 B). Names do NOT match registry exactly.

**Options**:
- `6A` — REPAIR_BY_FIND. Operator manually searches ChatGPT history / Downloads / external drive for the 5 named ZIPs. If found, operator copies them into `character_workflow/packages/`. Runner does NOT copy.
- `6B` — RENAME_UNDERSTANDING. Operator confirms the 2 unregistered ZIPs in AUDIO are functionally the same as registered #4 (`MIKAGE_CHARACTER_SCALE_LINEUP_V0_3_COMMANDER_LYRE_PATCH_PACKAGE.zip`) and #5 (`MIKAGE_LORA_VISUAL_FORM_BRIEF_V0_1_PACKAGE.zip`). Registry would be updated (text-only) to point to actual filenames. Requires `OPERATOR_APPROVED = YES` per registry edit.
- `6C` — DELETE_REGISTRY_ENTRIES. Operator confirms ZIPs are gone for good. Registry entries for the 5 ZIPs are marked `MISSING_NOT_RECOVERABLE`. Requires `OPERATOR_APPROVED = YES`.
- `6D` — DEFER. Registry stays as-is, mismatch documented.

**Runner recommendation**: `6D` (cannot verify ZIP equivalence without `OPEN ZIP` token, which has not been granted).

**Blocker impact**: Operator decisions 1, 2 might be informed by ZIP contents (Lyre DIRECTION_BRIEF ZIP almost certainly has the May 23 graphite/violet direction; could clarify drift).

`OPERATOR_APPROVED = `
`OPTION = `

---

## DECISION 7 — `commander_lyre.json` MISSING REFERENCE

**Question**: `MIKAGE_ZENITH_CANON_V2.md` (~line 587) references `commander_lyre.json` inside an ENTITIES tree. The file does not exist on any of the 8 mounted folders.

**Sources**:
- Canon V2 §8.2 "COMMANDER LYRE" section + ENTITIES tree reference to `commander_lyre.json`, `mikage_zenith.json`, `zenith_blade.json`, `unbreakable_shield.json`.
- `find -name commander_lyre.json` on all 8 mounts → 0 hits.

**Options**:
- `7A` — CREATE `commander_lyre.json` from Lyre brief + spec (machine-readable entity record). REQUIRES Decision 1 RESOLVED FIRST (need to know which visual direction to encode). Requires `OPERATOR_APPROVED = YES — OPTION = 7A`. Runner spec §1 rule 7 currently blocks until Lyre direction lock.
- `7B` — REMOVE the canon V2 reference to `commander_lyre.json`. Requires `OPERATOR_APPROVED = YES` to edit canon V2 (locked-tone canon document; CLAUDE.md hard rule "Do not approve canon" still applies — even this is text-only stripping).
- `7C` — DEFER. Document as known mismatch.

**Runner recommendation**: `7C`. (7A is blocked by Decision 1 + rule 7; 7B touches canon which the runner cannot do safely.)

**Blocker impact**: Stays as a known integrity mismatch in canon registry.

`OPERATOR_APPROVED = `
`OPTION = `

---

## 8. BLOCKED ACTIONS LIST (will not run until lifted)

| Action | Blocked by |
|---|---|
| Create `commander_lyre.json` | Rule 7 + Decision 1 unresolved |
| Render any Lyre image | `CLAUDE.md` lane hard rule (no render) + Decision 1 unresolved |
| Render any LORA image | `CLAUDE.md` lane hard rule + Decision 3 + Decision 4 unresolved |
| Open `D:\MIKAGE ZENITH AUDIO\00. COMPANNY LIMITED\02. COMMANDER LYRE\ChatGPT Image 21_16_17 27 thg 4, 2026.png` | Hard rule (no PNG open). Operator must open manually. Decision 2. |
| Open / extract any registered or unregistered ZIP | Rule 4. No `OPEN ZIP` token issued. |
| Copy `MIKAGE_LORA_INTERFACE_BOARD_V0_1_PACKAGE.zip` into `character_workflow/packages/` | Rule 5 (`IMPORT PNG` style token only; no equivalent `IMPORT ZIP`). Decision 6 unresolved. |
| Edit `CHARACTER_PACKAGE_REGISTRY_2026-05-23.md` to reflect Decision 6 | Rule 3 + per-file `OPERATOR_APPROVED = YES`. |
| Edit `MIKAGE_ZENITH_CANON_V2.md` to remove `commander_lyre.json` reference | Rule 3 + `CLAUDE.md` "Do not approve canon" + Decision 7 unresolved. |
| Cross-link LORA character with track 07 "THE ROOT ARCHITECT" | Rule 8 + Decision 5 unresolved. |
| Commit / push anything | Rule 10. No `COMMIT` token issued. |
| Move asset between folders | `CLAUDE.md` hard rule 8 (no rename / move / delete without approval). |

---

## 9. ONE SINGLE NEXT OPERATOR QUESTION

> **Q1.** Of the 7 decisions above, which ONE do you want resolved FIRST so the runner can proceed?
>
> Pick exactly one: `1` / `2` / `3` / `4` / `5` / `6` / `7`.
>
> Rationale guide (runner's view, not advice):
> - `1` unlocks `7` and clarifies which Lyre direction the drift goes to.
> - `2` requires operator to manually open 1 PNG, gives concrete evidence for `1`.
> - `3` + `4` together unblock all LORA text-only follow-up.
> - `5` is read-only on operator side (read 1 lyric file), unlocks LORA → track 07 audit if intentional.
> - `6` is heaviest (file recovery), unblocks nothing automatically but resolves the registry integrity gap.
> - `7` is fully blocked by `1`, do not pick first.

Answer below this line. Runner will read on next invocation.

```
Q1_ANSWER = 
```

— END OF DECISION BOARD V0.1 —
