# CHARACTER_OPERATOR_DECISION_BOARD_V0_5

Generated by: `CHARACTER_LANE_AUTONOMOUS_RUNNER_V0_1.md` (run #8)
Generated on: 2026-05-29
Mode: READ-ONLY. Focused on Decision 7 only, per operator `Q_NEXT = 7` in `CHARACTER_LORA_FACTION_LOCK_2026-05-29_F_REPORT.md`.
This board surfaces new evidence from Canon V2 §15 (DIRECTORY LAYOUT, just read for the first time in this lane) — which significantly expands Decision 7 scope. Runner did NOT edit any file.

---

## CONTEXT

Operator picked Decision 7 to resolve next: **`commander_lyre.json` missing reference**.

V0_1 listed 3 options (7A / 7B / 7C). Reading Canon V2 §15 reveals that the missing-JSON problem is BIGGER than just `commander_lyre.json` — canon V2 specifies a whole `ENTITIES/` folder that does not exist on disk. This board surfaces that scope and re-presents Decision 7 with new options (`7A_MIN`, `7A_PAIR`, `7A_FULL`, `7D_DRAFT_ONLY`) so operator can scale the answer to actual world-state.

---

## EVIDENCE BLOCK

### A. CANON V2 §15 DIRECTORY LAYOUT (verbatim, just read)

```text
MIKAGE_ZENITH/
│
├── CANON/
│   ├── MIKAGE_ZENITH_CANON_V2.md (THIS FILE)
│   ├── MIKAGE_PROMPT_LOCK.txt
│   ├── MIKAGE_NEGATIVE_LOCK.txt
│   └── MIKAGE_PASS_FAIL_CHECKLIST.md
│
├── ASSETS/
│   ├── reference_images/
│   ├── material_swatches/
│   └── lighting_presets/
│
├── ENTITIES/
│   ├── mikage_zenith.json
│   ├── commander_lyre.json
│   ├── zenith_blade.json
│   └── unbreakable_shield.json
│
├── WORLD/
│   ├── white_monolith.md
│   ├── neon_grid.md
│   └── entropy_economy.md
│
└── WORKFLOWS/
    ├── image_generation_pipeline.md
    ├── video_generation_pipeline.md
    └── quality_control_checklist.md
```

Header (just below this block): "**END OF CANON — NO DRIFT PERMITTED**" + §16 Authority: "**Status: Production Locked**" + "**Last Verified: 2026-03-19**".

### B. ACTUAL DISK STATE — `find` across 8 mounted folders

| Canon V2 path | Exists on disk? | Found at |
|---|---|---|
| `MIKAGE_ZENITH/` (top-level folder) | **NO** | — |
| `MIKAGE_ZENITH/CANON/` | **NO** (canon V2 file lives at V2 repo root, not under `MIKAGE_ZENITH/CANON/`) | `D:\KAGAMI-MZ_SYNC_PUSH_V2\MIKAGE_ZENITH_CANON_V2.md` (direct child of repo root) |
| `MIKAGE_ZENITH/ENTITIES/` | **NO** | — |
| `mikage_zenith.json` | **PARTIAL** — exists at `D:\workspace\ComfyUI\image_canon_gate\profiles\mikage_zenith.json` (ComfyUI profile). **CHUA_XAC_NHAN** whether it matches canon V2 entity schema. Runner did NOT open file to verify (would not violate any rule, but is out of scope for this board). | workspace/ComfyUI |
| `commander_lyre.json` | **NO** | — |
| `zenith_blade.json` | **NO** | — |
| `unbreakable_shield.json` | **NO** | — |
| `lora.json` | **NO** | — |
| `MIKAGE_PROMPT_LOCK.txt`, `MIKAGE_NEGATIVE_LOCK.txt` | **CHUA_XAC_NHAN** (not searched at this pass) | — |
| `MIKAGE_PASS_FAIL_CHECKLIST.md` | **YES** at `D:\KAGAMI-MZ_SYNC_PUSH_V2\MIKAGE_PASS_FAIL_CHECKLIST.md` (repo root, not under `MIKAGE_ZENITH/CANON/`) | V2 repo root |
| `WORLD/white_monolith.md`, `WORLD/neon_grid.md`, `WORLD/entropy_economy.md` | **CHUA_XAC_NHAN** | — |
| `WORKFLOWS/*.md` | **CHUA_XAC_NHAN** | — |

→ Canon V2 §15 directory layout is **largely aspirational**. The actual repo layout is flat at root, not nested under `MIKAGE_ZENITH/`. Only 1 of 4 entity JSONs has any candidate on disk (and it's in the ComfyUI runtime path, not the canon path).

### C. CANON V2 §8 ENTITY LIST vs §15 ENTITY JSON LIST (mismatch within canon V2 itself)

| §8 entity | §15 expected JSON |
|---|---|
| 8.1 Mikage Zenith | mikage_zenith.json ✓ listed |
| 8.2 Commander Lyre | commander_lyre.json ✓ listed |
| 8.3 ARCHON-IX & LYRA-0 | — (NOT listed in §15) |
| 8.4 Dr. Aris | — (NOT listed in §15) |
| 8.5 Tai Vane | — (NOT listed in §15) |
| (weapons) Zenith Blade | zenith_blade.json ✓ listed |
| (weapons) Unbreakable Shield | unbreakable_shield.json ✓ listed |

→ Canon V2 lists 5 character entities in §8 but only 2 character JSONs in §15. The other 3 (ARCHON-IX & LYRA-0, Dr. Aris, Tai Vane) have NO assigned JSON in canon V2's own directory plan. This is an internal canon V2 inconsistency — operator should know.

LORA: also NOT listed in §15 (since LORA was absent from canon V2 entirely until Lock 4F created the pending §7.0 update task).

### D. LOCKS ALREADY IN EFFECT THAT INFORM JSON DRAFTING

- **Lock 1A (Run #3)** — Lyre direction = Porcelain Minimalism, cyan, Molecular Monowire. Any `commander_lyre.json` MUST encode this, not the May 23 graphite/violet direction.
- **Lock 3A (Run #5)** — LORA `ANTHROPOMORPHIC_FORM = LOCKED = NONE`.
- **Lock 4F (Run #7)** — LORA `FACTION = META_SUBSTRATE`.

→ commander_lyre.json + a parallel lora.json have enough stable axes to be drafted text-only without contradiction.

---

## DECISION 7 — RE-PRESENTED (expanded options)

**Question**: How to resolve the `commander_lyre.json` missing reference — and given the broader §15 scope, how far should the runner go?

| ID | Direction | What runner produces | What's still NOT done | Risk |
|---|---|---|---|---|
| `7A_MIN` | Create ONLY `commander_lyre.json` | A new file `character_workflow/proposals/commander_lyre.json` encoding §8.2 + Lyre brief + visual spec + Lock 1A. Includes a header `"$canon_v2_path": "ENTITIES/commander_lyre.json"` indicating intended canonical destination. | `ENTITIES/` folder does not exist; runner will NOT create the canon-expected folder. Operator decides later when/where to move the file. zenith_blade.json + unbreakable_shield.json + lora.json + mikage_zenith.json reconciliation NOT done. | LOW. One file, in a proposals subfolder, easy to roll back. |
| `7A_PAIR` | Create `commander_lyre.json` + `lora.json` in parallel | Two files in `character_workflow/proposals/`. `lora.json` encodes Lock 3A (form) + Lock 4F (faction) + LORA brief + visual spec. | Same as 7A_MIN, plus weapon entity JSONs still missing. | LOW–MEDIUM. Two files in proposals folder. lora.json has no canon V2 §8 anchor yet (LORA not in §8) — file will reference pending §7.0 substrate update. |
| `7A_FULL` | Create all 4 missing entity JSONs from §15 + lora.json | Five files in `character_workflow/proposals/`: commander_lyre.json, zenith_blade.json, unbreakable_shield.json, lora.json, and an audit note for the existing workspace/ComfyUI mikage_zenith.json. | ENTITIES/ folder still not created. Drafting weapon JSONs (zenith_blade, unbreakable_shield) is mostly mechanical from canon V2 §8 + §11 item 4. | MEDIUM. Five files. Runner is doing more spec-derivation; chance of drift higher. |
| `7B` | REMOVE the canon V2 reference to `commander_lyre.json` (strip from §15) | Would require editing canon V2. | Canon V2 §15 + §16 say "NO DRIFT PERMITTED" + "Production Locked". | **BLOCKED by `CLAUDE.md` "Do not approve canon" + rule 3.** Runner refuses 7B. |
| `7C` | DEFER. | Nothing produced. Mismatch documented as known. | (no change) | None. Status quo. |
| `7D_DRAFT_ONLY` | Same as 7A_MIN but EXPLICITLY flagged as draft-only | Identical output to 7A_MIN with the file extension `.draft.json` and a top-level field `"$status": "DRAFT_PROPOSAL_NOT_CANON"`. Operator promotes to `.json` by hand. | Same as 7A_MIN. | LOWEST. Naming convention makes status obvious. |

### Runner's deterministic recommendation (default if no answer)

- By "minimize new files, minimize derivation risk" → `7D_DRAFT_ONLY` (one draft file, explicitly flagged, no canon edits).
- By "match operator earlier hint about parallel lora.json" → `7A_PAIR`.
- By "fix the whole §15 entity gap in one pass" → `7A_FULL`. NOT recommended unless operator wants the spec-derivation work in this lane.
- AVOID `7B` (blocked).
- `7C` is fine if operator wants to handle entity JSONs in a different workstream.

Runner default if no answer: `7C` (defer).

### Important constraints on any chosen mutation

If `7A_MIN`, `7A_PAIR`, `7A_FULL`, or `7D_DRAFT_ONLY` is approved:
- Runner will write files into `character_workflow/proposals/` — a NEW folder (does not currently exist). Runner will create this folder.
- Runner will NOT create `MIKAGE_ZENITH/ENTITIES/` folder (would require canon V2 update on layout, and `CLAUDE.md` blocks).
- Runner will NOT touch canon V2 §15.
- Runner will NOT touch the workspace/ComfyUI mikage_zenith.json (CLAUDE.md "Do not use ComfyUI runtime" + Do not move/rename without approval).
- Each draft JSON will include a `"$source"` array listing the exact files + sections it derived from, for traceability.

---

## ANSWER BLOCK

Fill in ONE of the following and save the file. Runner reads on next invocation.

```
DECISION_7_OPTION = 
DECISION_7_APPROVAL = 
```

Examples (do not interpret as recommendations):
- Draft only commander_lyre, explicit draft naming: `DECISION_7_OPTION = 7D_DRAFT_ONLY` AND `DECISION_7_APPROVAL = OPERATOR_APPROVED = YES`
- Draft commander_lyre + lora pair: `DECISION_7_OPTION = 7A_PAIR` AND `DECISION_7_APPROVAL = OPERATOR_APPROVED = YES`
- Draft all 4 + lora + workspace audit: `DECISION_7_OPTION = 7A_FULL` AND `DECISION_7_APPROVAL = OPERATOR_APPROVED = YES`
- Defer: `DECISION_7_OPTION = 7C` AND `DECISION_7_APPROVAL = OPERATOR_APPROVED = NO — DEFER`

If `7A_MIN`, `7A_PAIR`, `7A_FULL`, or `7D_DRAFT_ONLY` chosen with approval: runner creates the corresponding draft JSON(s) in `character_workflow/proposals/` PLUS a single lock report `CHARACTER_ENTITY_JSON_DRAFT_2026-05-29_<X>_REPORT.md` recording what was drafted and what was deliberately left undone.

If `7B` chosen: runner emits `CHARACTER_LANE_BLOCKED_BY_OPERATOR_DECISION_V0_1.md` quoting canon V2 §15 + §16 + `CLAUDE.md` "Do not approve canon" and explaining why 7B cannot proceed.

If `7C` or no approval token: runner records the deferral; nothing else changes.

---

## NOTHING ELSE TOUCHED

- Canon V2 — unchanged (read-only).
- All 5 LORA-relevant sources — unchanged.
- Lyre brief + spec + Lyre lock report — unchanged.
- LORA form lock + LORA faction lock — unchanged.
- May 23 handoff (AUDIO + AUDIO copy) — unchanged.
- V0_1, V0_2, V0_3, V0_4 boards — unchanged.
- `CLAUDE.md`, registry, Mikage bible — unchanged.
- workspace/ComfyUI/image_canon_gate/profiles/mikage_zenith.json — unchanged (existence noted, content NOT read at this pass).
- No ZIP opened.
- No PNG opened.
- No JSON created.
- No render.
- No commit.

— END OF DECISION BOARD V0.5 —
