# Deprecated Stack

`execution_lane/` is legacy execution infrastructure.

Active MIKAGE V2 control plane lives in:

- `MIKAGE/`
- `runtime/drive_queue/`

Do not treat this directory as the active control-plane source of truth.
