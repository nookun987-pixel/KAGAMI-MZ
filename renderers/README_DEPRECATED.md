# Deprecated Stack

`renderers/` is legacy rendering/runtime glue retained for migration support.

Active MIKAGE V2 queue runtime lives in:

- `runtime/drive_queue/runtime.js`
- `MIKAGE/lanes/image/image_executor.js`

Do not use this directory as the active control-plane architecture root.
