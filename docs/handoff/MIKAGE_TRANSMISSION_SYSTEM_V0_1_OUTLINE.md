# MIKAGE TRANSMISSION SYSTEM — V0.1 OUTLINE (Step 4 of Safe Plan)

Generated by: Claude Cowork (planning-only pass)
Generated on: 2026-05-29
Mode: OUTLINE ONLY. No new lore. No public copy. No new lore-drip posts. No tagline invention. No canon modification. No commit. No push. No render.
Scope: **Step 4 of `MIKAGE_IP_WORLD_BACKGROUND_PLAN_V0_1_SAFE_REVISION.md` §4 ONLY.**
Status: pending GPT review.

**Labeling convention** (used throughout):
- `[CONFIRMED_SOURCE]` — directly supported by source context (canon V2 / committed alignment / committed locks / committed design_system / committed lore-drip patch).
- `[CHUA_XAC_NHAN]` — unresolved / missing / not operator-approved.
- `[OUTLINE_ONLY]` — structural organization or directive-allowed terminology mapping, not a canon claim.

---

## 1. PURPOSE

`[OUTLINE_ONLY]`

This file is the **Step 4 Music / Transmission System outline** described in the Safe Plan §4. It captures how Mikage's music releases, cover art, short hooks, captions, and website/archive surfaces **function as one transmission system** — using only locked terminology, confirmed cadence, and CTA grammar already approved by the operator.

It is **not**:
- a public-facing document
- a new lore-drip post or replacement of the 6 approved drips
- a tagline-invention pass
- a release schedule
- a render or art task
- a canon update or canon-lock

It **is**:
- a planning anchor (Step 4 of a 7-step build-first plan)
- a structural mapping of existing release artifacts to the locked transmission frame
- preparation for GPT review prior to operator approval

---

## 2. SOURCE FILES READ

`[OUTLINE_ONLY]`

| # | Path | Role |
|---|---|---|
| 1 | `docs/handoff/MIKAGE_IP_WORLD_BACKGROUND_PLAN_V0_1_SAFE_REVISION.md` | Step 4 scope authority |
| 2 | `docs/handoff/MIKAGE_TAB_WEB_READ_FIRST_IP_WORLD_CONTEXT_V0_1.md` | Binding facts (CTA grammar, palette boundary, CHUA_XAC_NHAN, forbidden drift) |
| 3 | `docs/handoff/MIKAGE_IP_CORE_V0_1_OUTLINE.md` | Step 1 voice/anchor citations |
| 4 | `docs/handoff/MIKAGE_WORLD_BACKGROUND_WITHOUT_GEOGRAPHY_V0_1_OUTLINE.md` | Step 2 background discipline (no geography) |
| 5 | `docs/handoff/MIKAGE_CHARACTER_SYSTEM_V0_1_OUTLINE.md` | Step 3 entity grid |

Indirect (cited via the five files above; not re-opened at byte level):
- `docs/MIKAGE_WORLD_INTERFACE_ALIGNMENT_V0_2.md` §A–§F (transmission framing + CTA grammar + 5-section site IA)
- `docs/MIKAGE_LORE_DRIP_SERIES_ALIGNMENT_V0_2_PATCH.md` §3 (DRIP 6 transmission framing) + §5 (tagline `CHUA_XAC_NHAN` rules)
- `character_workflow/MIKAGE_PUBLIC_REVEAL_CANDIDATE_BOARD_V0_1.md` (committed `c34f15f`)
- `design_system/ui_kits/website/data.js` (committed in `a38f89a` — track catalog + locked CTA mapping)
- `design_system/colors_and_type.css` (committed in `a38f89a` — INTERFACE palette tokens)

---

## 3. TRANSMISSION SYSTEM DEFINITION

`[OUTLINE_ONLY]` for the framing; individual claims labeled inline.

The Mikage music + visual + caption + site stack is **one transmission system**. Each artifact has a role inside that system; no artifact stands alone.

`[CONFIRMED_SOURCE]` (alignment V0_2 §D + lore-drip patch §3 DRIP 1 + DRIP 6):
- A **track** = one **transmission**, numbered.
- The **archive** = **"Launch Arc"** of past transmissions.
- Hero header verbatim from the live site: `"Fifth transmission is live now."`

`[OUTLINE_ONLY]` directive-allowed structural mapping (per Step 4 ALLOWED list):
- **Track** → transmission (the signal).
- **Release** → archived signal (the logged transmission).
- **Cover** → signal shell (the visual carrier).
- **Short hook** → public fragment (a partial signal exposed).
- **Caption** → public decode (the readable annotation layer).
- **Website** → archive interface (the access surface).

`[OUTLINE_ONLY]` — these five mappings are the directive's authorized terminology for Step 4 planning. They are not canon terms unless operator later promotes them via the Step 5 Narrative Expansion Gate.

System rules (carried forward from Steps 1–3 + alignment V0_2):
- **Two-Canon boundary** stays intact: INTERFACE canon governs the website / UI / social / release pages / design system; ART canon governs render / character / film / image / blade / material / composition. The two do not mix at any transmission surface.
- **Public reveal gate** stays closed: 0 of 23 inventoried assets `APPROVED_FOR_PUBLIC`. Step 4 does NOT change this count.
- **WORLD page (#6)** stays `DEFERRED`. Step 4 does NOT introduce a WORLD surface.
- **No geography** at any layer.

---

## 4. CONFIRMED MAPPING TABLE

`[CONFIRMED_SOURCE]` for each row (citing alignment + design-system + canon V2 §10):

| Layer | Role in transmission system | Locked terminology / token | Source |
|---|---|---|---|
| Track (audio) | The transmission itself, numbered | `transmission`, `current transmission`, `Nth transmission is live now` | alignment §D + DRIP 1 |
| Release | The logged event when a transmission goes live | catalog number `T01..T30` + ISO date | design_system `data.js` track entries |
| Cover (square image) | The visual shell carrying the transmission identity | `cover` artwork; ART-canon palette for art; INTERFACE-canon for crops/composition on pages | Public Reveal Board §2 (presentation outputs) + design_system §F |
| Short hook (video clip) | A public fragment of one transmission | `short` (operator's existing folder convention `short/`, `short_reels_tasks/`); status fields like `*_SHORT*_VERIFY_REPORT.*` | inventory references via cross-repo grep V0_1 + handoff snapshot |
| Caption (text) | The public decode of a transmission for a specific platform | `caption`, `CLEAN_LYRIC_TOOLOST`, `platform captions` | inventory references (e.g., `GLASS_SKIN_SHORT1_PLATFORM_CAPTIONS.md`) — file existence confirmed; content per-track CHUA_XAC_NHAN until verified |
| Website (5 sections) | The archive interface | `HOME / TRANSMISSIONS / CHARACTER / MUSIC VISUAL / CANON` | alignment §F |
| HUD / typography | The signal voice across surfaces | `Cinzel` wordmark · `Shippori Mincho` headlines+CJK · `Space Mono` labels · canon V2 §10.1 Empire UI = Cold Cyan · Mikage HUD = `#E60000` (3D-wrapped, ≤15% frame) | design_system `colors_and_type.css` + canon V2 §10 |

`[OUTLINE_ONLY]` — the mapping table is a Step 4 structural recap. It does not promote any field to canon; canon V2 §15 + design_system already lock the lower-level facts.

---

## 5. TRACK / RELEASE LAYER

`[CONFIRMED_SOURCE]` per design_system `data.js` (committed `a38f89a`) — track catalog at time of commit:

| Status | Track no. | Tracks |
|---|---|---|
| live (operator-confirmed) | T01–T07 | THE LANDAUER PARADOX, DIGITAL ASH, THE BREACH, SINGULAR HEART, **PORCELAIN ASCENSION**, THE THEOREM, THE ROOT ARCHITECT |
| future (unreleased) | T08, T09, T10, T14, T17, T26 | GLASS SKIN, ガラスの肌, SLOW ORBIT, SIGNAL THIEF, 黑雨信號, 白瓷夜行 |
| uncertain | T30 | 本当の名前 |

`[CONFIRMED_SOURCE]` per lore-drip patch §3 DRIP 6 + alignment §D — tagline rule:
- **Only** `T05 PORCELAIN ASCENSION` carries a source-confirmed line: `"A white shell rises from the void."`
- All other tracks render `Tagline — UNCONFIRMED`.

`[CHUA_XAC_NHAN]`:
- Any track tagline beyond T05 — `CHUA_XAC_NHAN` (do NOT invent).
- Track release dates beyond what `data.js` lists — `CHUA_XAC_NHAN`.
- T30 status — locked as `uncertain` (per `data.js`); do NOT guess to `live` or `future` without operator confirmation.

`[OUTLINE_ONLY]` rules carried over:
- TITLES in `ALL CAPS` (per alignment §D casing rule).
- Catalog labels in `UPPERCASE` mono with wide tracking (`05 / LAUNCH ARC`).

---

## 6. COVER / VISUAL SHELL LAYER

`[CONFIRMED_SOURCE]` per Public Reveal Board §2 + design_system §F:

- Covers are the **square 3000×3000** TooLost-pack artworks per track (file naming convention: `<TITLE>_TOOLOST_3000.jpg` or `<TITLE>_TOOLOST_3000x3000.jpg` per inventory).
- Covers are rendered under **ART canon** (porcelain + void + controlled crimson + kintsugi gold).
- Cover usage **on a website/release-page surface** is rendered under **INTERFACE canon** (void + porcelain + violet signal only). The cover image itself stays ART; its surrounding chrome stays INTERFACE.

`[CONFIRMED_SOURCE]` per Public Reveal Board §1–§2:
- 4 Mikage core canon images (PRIMARY_V2 + 3 pose tests) — all `PENDING_OPERATOR_REVIEW`.
- 12 presentation outputs (jpg + 8 ChatGPT Images) — all `PENDING_OPERATOR_REVIEW`.
- 3 film-proof key visuals (UNIFIED_KEY_VISUAL_V4, AUDIO_SHORT_VISUAL_CANON_V4, ZENITH_BLADE_V2) — all `LOCKED — APPROVED_FOR_FILM_PROOF_SOURCE` and **NOT** approved for public reveal.

`[CHUA_XAC_NHAN]`:
- Specific cover for Lyre or LORA — `DOES_NOT_EXIST`.
- Operator approval for any cover to exit the board as `APPROVED_FOR_PUBLIC` — `0 of 23`.
- Cover treatment for future tracks not in the live T01–T07 set — `CHUA_XAC_NHAN`.

`[OUTLINE_ONLY]` — Step 4 does NOT propose new covers or change cover assignments. The shell layer is a structural recap of where covers fit in the transmission system.

---

## 7. SHORT HOOK / PUBLIC FRAGMENT LAYER

`[CONFIRMED_SOURCE]` per inventory references (cross-repo grep V0_1 + handoff `SHORT_AUDIO_REPAIR_FINAL_LOCK_REPORT`) — short hooks exist for several tracks as `short/` and `short_reels_tasks/` folder artifacts.

| Track | Short hook presence | Source |
|---|---|---|
| T05 PORCELAIN ASCENSION | `short_reels_tasks/PORCELAIN_ASCENSION_SHORT1_RENDER_PACKAGE.zip` | inventory |
| T07 THE ROOT ARCHITECT | `THE_ROOT_ARCHITECT_KINETIC_SHORT_HOOK_FROM_GPT_STILLS_V1.mp4` (+ contact sheet + verify report) | handoff `THE_ROOT_ARCHITECT_KEYFRAME_*` block |
| T08 GLASS SKIN | `GLASS_SKIN_SHORT1_*`, `GLASS_SKIN_SHORT2_*`, `GLASS_SKIN_SHORT3_*` | inventory |
| T10 ガラスの肌 (JP) | `ガラスの肌_SHORT1..4_FINAL_PACKAGE.zip` | inventory |
| T11 NO TOUCHDOWN | 3 short rebuilds (`SHORT1..3_CLUB_DANCE_FINAL_PACKAGE.zip`) | inventory |
| T12 SLOW ORBIT | `SLOW_ORBIT_SHORT1_0m15_0m47_FINAL_PACKAGE.zip` | inventory |
| T13 HUSH SAY LESS | multiple shorts | inventory |
| T17 NIGHT BITE | `NIGHT_BITE_SHORT_10S_PACKAGE.zip` | inventory |

`[CONFIRMED_SOURCE]` per handoff `SHORT_AUDIO_REPAIR_FINAL_LOCK_REPORT`:
- 26 repaired outputs PASS.
- 15 remux duplicates archived.
- 4 policy items still OPEN (do not reopen without operator selection).
- DO_NOT_REOPEN list active.

`[CHUA_XAC_NHAN]`:
- Which existing short hooks are `APPROVED_FOR_PUBLIC` — `CHUA_XAC_NHAN` (board does not enumerate shorts as a separate group; each public release of a short would need its own per-asset gate).
- Final approval of any pending short — `CHUA_XAC_NHAN` (4 policy items still open per handoff).
- Any short for a track not yet listed above — `CHUA_XAC_NHAN`.

`[OUTLINE_ONLY]` — Step 4 does NOT propose new short hooks or repair tasks. The fragment layer is a structural recap.

---

## 8. CAPTION / PUBLIC DECODE LAYER

`[CONFIRMED_SOURCE]` per alignment §D + lore-drip patch §3:
- Outward voice = calm · minimal · mysterious · weighted · non-salesy · **no emoji** · **no hype punctuation**.
- TITLES in `ALL CAPS`. Sentences in normal case. Mono labels in `UPPERCASE` wide-tracking.
- CTA is locked grammar (§10 below).
- No "out now" if not live-confirmed. No `Pre-save / Listen` mix.
- Person: third-person and impersonal ("Listen now"), occasionally collective. Almost never "I".

`[CONFIRMED_SOURCE]` per inventory references — caption file class:
- `<TITLE>_CLEAN_LYRIC_TOOLOST.txt` — per-track lyric caption for TooLost ingest.
- `<TITLE>_SHORT*_PLATFORM_CAPTIONS.md` — short-hook platform captions (e.g., `GLASS_SKIN_SHORT1_PLATFORM_CAPTIONS.md`).
- `<TITLE>_SHORT*_VERIFY_REPORT.*` — caption verification reports.

`[CHUA_XAC_NHAN]`:
- Public approval of any caption for release — `CHUA_XAC_NHAN` per board (no public copy is approved unless operator approves per-row).
- Caption variants for new platforms / new tracks — `CHUA_XAC_NHAN`.

`[OUTLINE_ONLY]`:
- The directive-allowed mapping `caption = public decode` is a structural framing for Step 4 planning. The actual caption files retain their existing on-disk naming conventions.

---

## 9. WEBSITE / ARCHIVE INTERFACE LAYER

`[CONFIRMED_SOURCE]` per alignment §F + design_system `ui_kits/website/`:

5 site sections (locked at alignment §F):

1. **HOME** — current transmission + signal
2. **TRANSMISSIONS** — Launch Arc archive
3. **CHARACTER** — Mikage helmet / sealed one / 鏡
4. **MUSIC VISUAL** — silent loop field / signal motif
5. **CANON** — rules, proof, protected doctrine

WORLD page (#6): `DEFERRED` per alignment §F. **Not built. Not planned at Step 4.**

`[CONFIRMED_SOURCE]` per design_system `colors_and_type.css` (committed `a38f89a`):
- INTERFACE palette only: void `#050508`, porcelain `#f2eeea`, violet `#8F00FF` (signal only, never fill).
- Three font families locked: Cinzel (wordmark), Shippori Mincho (headlines + CJK), Space Mono (labels).
- Tracking, spacing (8pt base), radius (near-zero, max 2px for inputs), motion (slow, controlled, no bounce) all locked.

`[CONFIRMED_SOURCE]` per design_system `ui_kits/website/data.js`:
- `MZ_CTA(track)` function locked: `live → "Listen now"`, `uncertain → "Link"`, otherwise (future) → `"Pre-save"`.
- 14 tracks in the catalog at commit time.
- `logoStatus = "UNCONFIRMED"`, `wordmarkStatus = "DRAFT"`.

`[CHUA_XAC_NHAN]`:
- Any new section beyond the 5 locked sections — `CHUA_XAC_NHAN`.
- WORLD page activation — `CHUA_XAC_NHAN` / `DEFERRED`.
- Logo asset for public use — `UNCONFIRMED` per data.js + brand README.

`[OUTLINE_ONLY]`:
- Section IA is the existing locked one; Step 4 does NOT add a new section.

---

## 10. CTA GRAMMAR

`[CONFIRMED_SOURCE]` per alignment §D + lore-drip patch §3 DRIP 6 + design_system `data.js` `MZ_CTA`:

| Track status | Locked CTA wording | Visual treatment |
|---|---|---|
| `live` | exactly `Listen now` | primary button + arrow `→` + glowing violet dot |
| `future` | exactly `Pre-save` | ghost / secondary button |
| `uncertain` | exactly `Link` | ghost link, lowest emphasis |
| unknown line | render `Tagline — UNCONFIRMED` (mono micro-label) | label color `var(--silver-dim)` |

**Forbidden** (verbatim, hard):
- `Pre-save / Listen` (mixed wording)
- "🔥" or any emoji
- "out now" when not live-confirmed
- "AI banger"
- "viral hit"
- "new drop!!!"
- generic cyberpunk hype copy

`[OUTLINE_ONLY]` — Step 4 does NOT propose any new CTA wording. The grammar is locked verbatim at alignment §D and at the `MZ_CTA` function in `data.js`.

---

## 11. TWO-CANON BOUNDARY FOR TRANSMISSION SURFACES

`[CONFIRMED_SOURCE]` per alignment V0_2 §B + IP-core §6 + design_system colors_and_type.css:

| Transmission surface | Layer | Palette tokens | Crimson / kintsugi gold / Z-Blue? |
|---|---|---|---|
| Track audio | (no visual layer) | n/a | n/a |
| Cover image (the artwork itself) | ART canon | porcelain #FAFAFA + void #0A0A0A + crimson #E60000 + kintsugi gold (seams) | YES, per ART canon |
| Cover usage on site/page (chrome around the image) | INTERFACE canon | void #050508 + porcelain #f2eeea + violet #8F00FF (signal only) | NO, never |
| Short hook video (render frames) | ART canon | ART palette | YES, per ART canon |
| Short hook caption + UI | INTERFACE canon | INTERFACE palette | NO |
| Caption text | INTERFACE canon | n/a (typography only) | NO |
| Website (5 sections) | INTERFACE canon | INTERFACE palette only | NO |
| Release page (per track) | INTERFACE canon | INTERFACE palette only; cover image embedded stays ART | NO at chrome layer |
| Social post art (if/when) | ART canon for the image; INTERFACE canon for any chrome/caption | both layers, never mixed at the same byte | per-layer rule |

`[OUTLINE_ONLY]` — the rule is "the two do not mix at any transmission surface." Crimson / kintsugi gold / Z-Blue never enter INTERFACE chrome; violet signal stays interface-only.

---

## 12. CHUA_XAC_NHAN ITEMS

`[CONFIRMED_SOURCE]` — preserved verbatim from read-first §9 + IP-core §7 + Step 2 §8 + Step 3 §13. Step 4 promotes nothing. Re-listed with Step-4 extensions:

Carried forward:
- Unbreakable Shield physicality (`WEAPON_DRIFT_001`)
- LORA "Root Architect" PUBLIC framing
- Location seeds (any kind)
- Human society structure
- City geography
- Factions beyond Three Ideologies
- WORLD page (#6)
- Z-Blue hex / use case
- Mikage production prompt seed
- Voice / dialogue profiles for all six entities
- Commander Lyre / LORA visual assets (`DOES_NOT_EXIST`)
- Clean Digital Gold hex
- Mikage / Lyre official height lock (both provisional)

Added at Step 4:
- Track taglines beyond T05 PORCELAIN ASCENSION — `CHUA_XAC_NHAN`.
- Public approval status for any short hook — `CHUA_XAC_NHAN` (board does not enumerate shorts; per-asset gate applies).
- Public approval status for any caption — `CHUA_XAC_NHAN`.
- Release status of T30 本当の名前 — locked as `uncertain` (do NOT promote to `live` or `future`).
- Specific release dates beyond `data.js` entries — `CHUA_XAC_NHAN`.
- Logo asset for public use — `UNCONFIRMED` (per `data.js` `logoStatus`).
- Wordmark final art — `DRAFT` (per `data.js` `wordmarkStatus`).
- 4 open policy items from `SHORT_AUDIO_REPAIR_FINAL_LOCK_REPORT` — stay OPEN, do NOT auto-reopen.

→ Treat each as `CHUA_XAC_NHAN` whenever it appears in downstream planning, briefs, or copy.

---

## 13. FORBIDDEN DRIFT

`[CONFIRMED_SOURCE]` — hard exclusion filter inherited from read-first §10 + IP-core §8 + Step 2 §9 + Step 3 §14. This Step 4 outline introduces ZERO new forbidden items and removes ZERO existing ones.

Step-4-specific application of the filter:
- No "out now" copy for any track not in the `live` status set per `data.js`.
- No `Pre-save / Listen` mix.
- No emoji in any transmission surface.
- No invented per-track tagline beyond T05 PORCELAIN ASCENSION.
- No new lore-drip post (the 6 approved drips are `APPROVED_FOR_CADENCE`; no new drips proposed at Step 4).
- No WORLD page IA.
- No release status guess (T30 stays `uncertain`; future tracks stay `future`).
- No public reveal authorization (board count `0 / 23` unchanged by Step 4).
- No hype language / "AI banger" / "viral hit" / "new drop!!!" / generic cyberpunk.
- No public ART-only color (crimson / kintsugi gold / Z-Blue) in interface chrome.

If any downstream content reads as introducing one of those items at the transmission layer, the content is rejected at this Step 4 layer.

---

## 14. NEXT SAFE TASK

```text
GPT_REVIEW_STEP_4_TRANSMISSION_SYSTEM_OUTLINE_BEFORE_OPERATOR_COMMIT
```

After GPT review:
- If approved → operator decides whether to commit this Step 4 outline in an isolated micro-commit (suggested message: `docs: add Mikage transmission system V0.1 outline (Step 4)`).
- If revision requested → patch this file, do not rewrite.
- Step 5 (Narrative Expansion Gate) does NOT start until Step 4 is operator-approved and committed.

Reminder: per the patch ordering in `MIKAGE_IP_WORLD_BACKGROUND_PLAN_V0_1_SAFE_REVISION.md` §4, **Step 5 Narrative Expansion Gate must come before Step 6 Location Seeds**. Step 4 → 5 → 6 → 7 sequencing is enforced.

---

## WHAT CLAUDE COWORK DID NOT DO THIS PASS

- ✗ Invent any new transmission, release, track, cover, short, caption, or section
- ✗ Promote any track from `future` / `uncertain` to `live` (or vice versa)
- ✗ Invent any per-track tagline beyond T05 PORCELAIN ASCENSION
- ✗ Propose a new lore-drip post
- ✗ Add a WORLD page or any 6th site section
- ✗ Authorize any asset for public reveal (board stays `0 / 23`)
- ✗ Edit canon V2 or any committed file
- ✗ Touch the Forbidden Drift list
- ✗ Render any art
- ✗ Commit anything
- ✗ Push anything
- ✗ Write more than one file
- ✗ Re-open canon V2 / Mikage Bible / Lyre brief / LORA brief / alignment / lore-drip patch at the byte level (cited through Steps 1–3 + indirect references)

Only one operation: write this single Step 4 outline file at `docs/handoff/MIKAGE_TRANSMISSION_SYSTEM_V0_1_OUTLINE.md`.

---

```text
STATUS = STEP_4_TRANSMISSION_SYSTEM_OUTLINE_WRITTEN_FOR_GPT_REVIEW
NEXT_SAFE_TASK = GPT_REVIEW_STEP_4_TRANSMISSION_SYSTEM_OUTLINE_BEFORE_OPERATOR_COMMIT
```

*End of MIKAGE_TRANSMISSION_SYSTEM_V0_1_OUTLINE.md*
*Step 4 of 7. Planning only. No new lore. No public copy. No tagline invention. No canon change. No commit. No push.*
