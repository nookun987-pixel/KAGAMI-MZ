# MIKAGE_RENDER_GATE_FAL_V0_1 — OPERATOR UNLOCK CHECKLIST

Generated by: Claude Cowork (Mikage repo operator)
Generated on: 2026-05-31
Status: **DRAFT_PROPOSAL_NOT_CANON · OPERATOR_GATED**
Tool: `tools/render_gate/mikage_render_gate_fal.py`
Config: `tools/render_gate/mikage_render_gate_config.json`
README: `docs/automation/render_briefs/MIKAGE_RENDER_GATE_FAL_V0_1_README.md`
Implementation report: `docs/handoff/MIKAGE_RENDER_GATE_FAL_V0_1_IMPLEMENTATION_REPORT.md`

> Single-page operator checklist to unlock V0.1 from its shipped safety stop and run a real FAL render. Every step is operator-only. Cowork does not edit config, brief tokens, or env vars. Cowork does not call FAL. Cowork does not mark any render PASS.

---

## PREREQUISITE — V0.1 finalization is green

Before Step 1, confirm these are all true (Cowork has verified them on 2026-05-31; re-verify if state may have changed):

```text
py_compile                                          = PASS
config.model_id                                     = "CHUA_XAC_NHAN"
images under docs/automation/render_briefs/runs/    = NONE
brief sha256 (Lyre Phase 1 V0.1 JSON)               = 61aaad0824c65ea0cc3760bac2389ee19158b76ea4984792195f74000fb1cb1a
canon files modified                                = NO
brief MD + JSON modified                            = NO
attempt_001 report status                           = DRY_RUN_PASS_NO_API_CALL
attempt_002 report status                           = RENDER_BLOCKED_OPERATOR_APPROVAL_REQUIRED
api_called in both reports                          = false
```

If any of these is not true, STOP. Resolve before proceeding.

---

## STEP 1 — Choose `model_id` manually

Operator-only. Cowork must not pick.

Decision is between concrete FAL model ids. Common choices the operator may consider (NOT a recommendation):

- `fal-ai/flux/dev`
- `fal-ai/flux/schnell`
- `fal-ai/flux-pro/v1.1`
- `fal-ai/stable-diffusion-v35-large`
- (or any other FAL-hosted image model the operator has access to)

Considerations the operator should weigh, not Cowork:

- Cost per image
- Aspect ratio support (brief asks for 2.35:1 / 3360×1430)
- Response shape compatibility (V0.1 `extract_image_url` covers the three common shapes; unusual shapes may need a V0.2 adapter)
- Prompt-following quality on industrial / non-anime aesthetics

Record the chosen model id somewhere persistent (operator's local note or session log). The model id will be pasted into the config in Step 2.

---

## STEP 2 — Edit `tools/render_gate/mikage_render_gate_config.json`

Replace exactly one field:

```diff
- "model_id": "CHUA_XAC_NHAN",
+ "model_id": "<chosen-model-id-from-Step-1>",
```

Do NOT change other fields unless the operator has a specific reason. In particular:

- `provider` stays `"fal"`
- `output_root` stays `"docs/automation/render_briefs/runs"`
- `dry_run_default` stays `true` (it is currently unused by the V0.1 CLI but is preserved for future versions)
- `fal_endpoint_template` stays `"https://fal.run/{model_id}"` unless FAL changes its base URL
- `fal_timeout_seconds` may be raised if the chosen model is slow

After saving, re-run a `--dry-run` to confirm the JSON still parses (see Step 5).

---

## STEP 3 — Set `operator_approval_token` in the target brief JSON

Edit ONLY `project_metadata.operator_approval_token` in the target brief JSON. For the V0.1 compatibility target:

```text
docs/automation/render_briefs/BRIEF_COMMANDER_LYRE_PHASE_1_IMPERIAL_DUTY_V0_1.json
```

Change:

```diff
- "operator_approval_token": null,
+ "operator_approval_token": "<token-string>",
```

The token string is operator-issued (e.g. a date-id like `2026-05-31_LYRE_P1_RUN_01` or any other identifier the operator finds useful for audit). It is not validated against any registry — its presence is what unlocks the gate.

Do NOT change any other field in the brief. The brief sha256 will (intentionally) change after this edit; the new value is recorded in the next run's attempt report under `source_brief_sha256_before/after`. Cowork uses the new value as the new baseline.

---

## STEP 4 — Export `FAL_KEY` (or `FAL_API_KEY`)

Operator's shell, before invoking the tool:

```bash
export FAL_KEY="<operator's FAL API key>"
# OR equivalently:
export FAL_API_KEY="<operator's FAL API key>"
```

Either env var name works. The tool checks both. Do not commit the key to the repo. Do not paste the key into any brief or report.

---

## STEP 5 — Run dry-run one more time

This is the final pre-flight. Run from the repo root:

```bash
python tools/render_gate/mikage_render_gate_fal.py \
    --brief docs/automation/render_briefs/BRIEF_COMMANDER_LYRE_PHASE_1_IMPERIAL_DUTY_V0_1.json \
    --dry-run
```

Expected outcomes (all must hold before Step 6):

- Exit code 0
- `STATUS = DRY_RUN_PASS_NO_API_CALL`
- `API_CALLED = false`
- A new `attempt_NNN/` folder is created
- `prompt_compact.txt`, `negative_compact.txt`, `fal_request.json`, `render_attempt_report.json`, `render_attempt_report.md` are written
- `fal_response.json` and `output.png` are NOT written
- `source_brief_unchanged = true` in the JSON report
- Open `fal_request.json` in that folder and visually confirm:
  - `"$intended_model_id"` shows the chosen model id from Step 1
  - `"image_size"` is `{"width": 3360, "height": 1430}` (or whatever the operator wants; V0.2 may let the operator override per-run)
  - `"prompt"` and `"negative_prompt"` look correct

If any check fails, STOP and diagnose. Do not proceed to Step 6.

---

## STEP 6 — Run the real render

Only if Step 5 passes cleanly:

```bash
python tools/render_gate/mikage_render_gate_fal.py \
    --brief docs/automation/render_briefs/BRIEF_COMMANDER_LYRE_PHASE_1_IMPERIAL_DUTY_V0_1.json
```

(no `--dry-run` flag)

Expected on success:

- Exit code 0
- `STATUS = RENDER_OUTPUT_CREATED_VERIFY_NOT_STARTED`
- `API_CALLED = true`
- `attempt_NNN/output.png` exists
- `attempt_NNN/fal_response.json` exists
- `attempt_NNN/render_attempt_report.json` carries `verify_status = "NOT_STARTED"`, `anchor_established = "NO"`, `canon_lock = "NO"`, `asset_lock = "NO"`

Expected on FAL failure:

- Exit code 0 (the tool exits cleanly even on API failure)
- `STATUS = FAL_API_REQUEST_FAILED`
- `attempt_NNN/output.png` does NOT exist
- `attempt_NNN/fal_response.json` exists with whatever body FAL returned

Either way, the source brief sha256 must be unchanged (i.e. the post-edit sha256 from Step 3).

---

## STEP 7 — After `output.png` exists, ask Cowork to draft a verification report

Operator-only signal. Do not skip.

The tool itself stamps `verify_status = "NOT_STARTED"` in every report. It NEVER marks a render PASS. Verification is a separate Cowork deliverable, drafted on operator request, against the brief's `verification_checklist` (10 checks for the Lyre V0.1 brief: F1/F2/F3/F4/ERA_TAG_MATCH/NEGATIVE_LOCK_INHERITED/SHIELD_HYBRID_PHASE_1_DORMANT/MONOWIRE_HOLSTERED/ANCHOR_FIRST_ESTABLISHMENT/LICENSING_CLEAN).

Cowork's verification report will:

- Apply every check in the checklist
- Report PASS / FAIL / PASS_WITH_NOTE per check
- NOT mark the overall render PASS — operator decides

After operator reviews Cowork's findings, the operator (not Cowork) decides:

- APPROVE → operator promotes the image to the brief's `output_target.output_path`, updates `proposals/commander_lyre.json` if relevant, and declares `anchor_lyre_001` established. Asset-lock + canon-edit are separate operator-only steps.
- REVISE → Cowork drafts brief V0.2 with revision notes; V0.1 preserved as audit trail.
- REJECT → output stays in the run folder; new attempt next session.

Until Step 7 finishes, **the render is not approved, not verified, not anchored, not asset-locked, and not canon-ready.** Do not promote it externally (Spotify cover, MV frame, social post, merch) before that gate.

---

## HARD STOPS (do NOT do any of these from this checklist)

- Do NOT skip Step 5 (dry-run before real render)
- Do NOT mark any render PASS automatically
- Do NOT establish `anchor_lyre_001` from the tool — only operator promotes after verification
- Do NOT copy `output.png` to the brief's `output_target.output_path` without verification report + operator approval
- Do NOT patch Canon V2 from this checklist (canon edits are separate operator-only tasks)
- Do NOT asset-lock from this checklist
- Do NOT batch process multiple briefs — V0.1 is one-brief-one-run; batching is a V0.2 question
- Do NOT extend the tool's behavior from this checklist — V0.2 routing / multi-brief compact prompt / per-model adapters are scoped separately

---

## ROLLBACK

If anything goes wrong (wrong model id, broken JSON, accidental commit):

1. Restore `tools/render_gate/mikage_render_gate_config.json` `model_id` to `"CHUA_XAC_NHAN"`.
2. Restore `project_metadata.operator_approval_token` to `null` in the brief JSON.
3. Unset env var: `unset FAL_KEY FAL_API_KEY`.
4. Delete the offending `attempt_NNN/` folder.
5. Re-verify the 8 prerequisite values at the top of this file.

---

## NEXT_SAFE_TASK

```text
NEXT_SAFE_TASK = OPERATOR_FOLLOWS_STEP_1_THROUGH_STEP_7_AT_OWN_PACE
SAFE_TO_RUN_DRY_RUN_AT_ANY_TIME = YES
SAFE_TO_RUN_REAL_RENDER = ONLY_AFTER_STEPS_1_THROUGH_4_COMPLETE
COWORK_NEXT_DELIVERABLE = VERIFICATION_REPORT (only when operator requests, only after output.png exists)
```

---

*End of MIKAGE_RENDER_GATE_FAL_V0_1_OPERATOR_UNLOCK_CHECKLIST.md*
*7 steps. Operator-only. No render claimed. No anchor established. No canon edited. No asset locked.*
