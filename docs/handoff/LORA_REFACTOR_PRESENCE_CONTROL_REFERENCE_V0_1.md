# LORA REFACTOR PRESENCE — CONTROL REFERENCE — V0.1

Generated by: Claude Cowork (Mikage repo operator — Character Cast Lane · Hybrid Approach pivot)
Generated on: 2026-05-31
Status: **DRAFT_PROPOSAL_NOT_CANON · OPERATOR_REVIEW_PENDING**
Companion to: `BRIEF_LORA_REFACTOR_PRESENCE_V0_1.{md,json}` + `LYRE_CONTROL_REFERENCE_V0_1.md` (Lyre lockset format mirrored here).

> **11 visual locks for LORA Phase 1 Baseline.** Unlike Lyre's 11 locks (which were *symptoms* of model-family-independent body failures), LORA's 11 locks are *primary spec requirements* the model must satisfy on first prompt-only attempt. LORA has `anthropomorphic_form = LOCKED_NONE` so the body-failure modes don't apply — but new substrate-failure modes (warm gold creep, glitch creep, neutral-gray void replacement, multi-segment cyan habit, ring asymmetry) need their own locks.

---

## 1. CONTEXT

```text
SUBJECT                = LORA (entity_id = lora) — Refactor Substrate
PHASE                  = 1 (Baseline · pre-Golden-Patch)
SCALE_RULE             = ARCHITECTURAL (environment-as-subject)
ANTHROPOMORPHIC_FORM   = LOCKED_NONE
ERA                    = V2_5_LORA_REFACTOR
RENDER_GATE_BRIEF      = docs/automation/render_briefs/BRIEF_LORA_REFACTOR_PRESENCE_V0_1.json
COMPACT_PROMPT_SOURCE  = tools/render_gate/mikage_render_gate_fal.py :: COMPACT_PROMPT_V0_1_LORA_REFACTOR_PRESENCE
COMPACT_PROMPT_MODE    = compact_prompt_v0_1_lora_presence
PROMPT_ONLY_FIRST_TRY  = YES (no control conditioning required for first attempt — pivot rationale is precisely that LORA matches flux defaults better than Lyre)
LOCK_SET_PURPOSE       = First-try visual locks for prompt-only flux-pro/v1.1 attempt; mirrors Lyre lockset format for verification consistency.
```

---

## 2. THE 11 LOCKS

### LOCK 1 — No humanoid figure

```text
ID         = LOCK_01_NO_HUMANOID_FIGURE
LANGUAGE   = ZERO humanoid bodies, characters, persons, faces, anatomy anywhere in the frame at any
            scale. The frame contains the Enso ring + the void only.
EVIDENCE_FROM_PRIOR_LORA_RENDERS = none (first render attempt for LORA)
LYRE_PARALLEL = This is what made Lyre prompt-only attempts hard. LORA's form spec eliminates this risk entirely.
MAPS_TO_BRIEF_CHECK = L1_NO_HUMANOID_FIGURE
```

### LOCK 2 — White void dominant porcelain-#FAFAFA

```text
ID         = LOCK_02_WHITE_VOID_DOMINANT
LANGUAGE   = The background is uniform porcelain-white #FAFAFA infinite void. No horizon, no
            floor, no ceiling, no wall, no architecture behind the ring. The white field is the
            primary spatial medium.
RISK       = flux-pro defaults to "neutral gray studio backdrop" (observed in Lyre attempt_006).
            Must specify "porcelain-white #FAFAFA" explicitly + "no studio gray" in negative.
MAPS_TO_BRIEF_CHECK = L2_WHITE_VOID_DOMINANT
```

### LOCK 3 — Mechanical Enso ring central architectural

```text
ID         = LOCK_03_MECHANICAL_ENSO_RING_CENTRAL
LANGUAGE   = ONE Enso ring, perfect single calligraphic circle stroke, geometric center coincides
            with frame center. Ring height occupies 55-75% of frame height. ONE ring only — no
            multiple rings, no concentric pairs, no broken arcs, no partial circles.
RISK       = flux may render "decorative ring composition" with multiple concentric rings, or
            interpret "Enso" as Japanese calligraphy poster (text + ink splash).
ANTI_DRIFT = "single calligraphic stroke", "ONE continuous ring", "no kintsugi seams (that is Mikage)"
MAPS_TO_BRIEF_CHECK = L3_MECHANICAL_ENSO_RING_CENTRAL
```

### LOCK 4 — Cold-cyan sync light thin single inner-edge line

```text
ID         = LOCK_04_COLD_CYAN_SYNC_LIGHT_THIN
LANGUAGE   = ONE thin cold-cyan #00C8FF line traces the inner circumference of the ring. Uniform
            thickness. No flicker. No break. No additional cyan accents anywhere else on the ring
            or in the void.
RISK       = flux-pro habit of adding indicator-style accent lights (observed in Lyre attempt_006 cyan
            knee circles + chest hex). Same habit could produce multi-accent ring (cyan dots at
            cardinal points, cyan inner-glow halo, etc.).
ANTI_DRIFT = "single thin line on inner edge only", "no additional cyan accents", "no indicator dots"
MAPS_TO_BRIEF_CHECK = L4_COLD_CYAN_SYNC_LIGHT_THIN
```

### LOCK 5 — Absolute bilateral symmetry

```text
ID         = LOCK_05_ABSOLUTE_BILATERAL_SYMMETRY
LANGUAGE   = Frame is mirror-symmetric across the vertical axis. Left half is pixel-perfect
            mirror of right half. Ring's geometric center sits exactly on frame's vertical center
            axis.
RISK       = flux can drift toward slight diagonal compositions or off-center "interesting" framing.
            Explicit "absolute bilateral symmetry" plus "centered" plus "static" in prompt.
MAPS_TO_BRIEF_CHECK = L5_ABSOLUTE_BILATERAL_SYMMETRY
```

### LOCK 6 — No Golden Patch in Phase 1

```text
ID         = LOCK_06_NO_GOLDEN_PATCH_PHASE_1
LANGUAGE   = Phase 1 Baseline = pre-intervention. NO clean digital gold accent anywhere in the
            frame. NO surgical gold patch event. The Golden Patch is the SUBJECT of a future
            separate brief (BRIEF_LORA_GOLDEN_PATCH_EVENT_V0_1, pending Clean Digital Gold hex
            canon-lock).
RISK       = Operator's lora.json mentions Golden Patch as a primary motif. flux may infer
            "render the LORA gold accent" even though Phase 1 is pre-intervention. Explicit
            negation in prompt + Phase 1 framing.
MAPS_TO_BRIEF_CHECK = L6_NO_GOLDEN_PATCH_PHASE_1
```

### LOCK 7 — No warm color anywhere

```text
ID         = LOCK_07_NO_WARM_COLOR_ANYWHERE
LANGUAGE   = ZERO warm color in the frame: no amber #FFAA33, no orange #FF8000, no warm gold
            #B8860B, no hero gold, no fantasy gold, no warm yellow, no sunset, no crimson #E60000.
EVIDENCE_FROM_LYRE_AUDIT_TRAIL:
  - flux/dev has documented amber-embellishment habit (Lyre V0.1 a04 amber knee lights,
    Lyre V0.2 a04 amber hip inserts)
  - flux-pro improved on this (Lyre V0.2 a06 had no amber)
  - LORA's "Clean Digital Gold" exists in canon BUT only as a Golden Patch event accent,
    NOT in Phase 1 baseline; ban gold entirely in this brief
MAPS_TO_BRIEF_CHECK = L7_NO_WARM_COLOR_ANYWHERE
```

### LOCK 8 — No violet no glitch no motion blur

```text
ID         = LOCK_08_NO_VIOLET_NO_GLITCH
LANGUAGE   = ZERO electric violet #7A00FF, ZERO neon purple, ZERO glitch bands, ZERO motion blur,
            ZERO ghost trails, ZERO spawn copies, ZERO fiber-optic dress motif.
RATIONALE  = All of the above are LYRA signatures (LYRA = ARCHON-IX vessel, V1 era). LORA is
            V2.5 substrate — opposite end of the axis. LYRA's chaos motifs must NEVER appear
            on a LORA render.
DISAMBIGUATION_REFERENCE = character_workflow/proposals/lyra.json _DISAMBIGUATION_NOT_TO_BE_CONFUSED_WITH
MAPS_TO_BRIEF_CHECK = L8_NO_VIOLET_NO_GLITCH
```

### LOCK 9 — Architectural scale not prop

```text
ID         = LOCK_09_ARCHITECTURAL_SCALE_NOT_PROP
LANGUAGE   = The ring reads as cathedral-sized / particle-accelerator-sized / temple-gate-sized.
            NOT jewelry. NOT prop. NOT handheld. NOT wristwatch dial. Implied scale anchor =
            "monumental object suspended in a void".
RISK       = flux may interpret "ring" as accessory/jewelry/wedding-band scale.
ANTI_DRIFT = "architectural scale", "monumental", "occupying 60-70% of frame height",
            "cathedral-sized", "no jewelry framing"
MAPS_TO_BRIEF_CHECK = L9_ARCHITECTURAL_SCALE_NOT_PROP
```

### LOCK 10 — Hyper-real B4C porcelain material

```text
ID         = LOCK_10_HYPER_REAL_MATERIAL
LANGUAGE   = Brushed B4C porcelain ceramic on the ring surface. Matte low-specular finish.
            Subtle micro-grain visible. Shallow subsurface scattering on the porcelain. Hyper-real
            material rendering. Industrial precision finish. NOT vinyl. NOT glossy plastic.
            NOT chrome. NOT mirror (no environment to reflect anyway).
EVIDENCE_FROM_LYRE_AUDIT_TRAIL:
  - flux-pro/v1.1 documented strong matte-ceramic discipline (Lyre V0.2 a06 F1 PASS).
  - This lock is expected to PASS on first attempt under flux-pro.
MAPS_TO_BRIEF_CHECK = L10_HYPER_REAL_MATERIAL
```

### LOCK 11 — No studio gray background

```text
ID         = LOCK_11_NO_STUDIO_GRAY_BACKGROUND
LANGUAGE   = The void does NOT default to neutral gray / studio product backdrop / Halo Spartan
            product framing. The void MUST be porcelain-white #FAFAFA — the LORA spatial signature.
COMPANION_TO = LOCK_02 (positive spec); LOCK_11 is the negation companion that names the specific
              failure pattern flux-pro exhibited on Lyre attempt_006.
EVIDENCE_FROM_LYRE_AUDIT_TRAIL = Lyre V0.2 a06 ERA_TAG_MATCH FAIL (flux-pro defaulted to studio gray).
ANTI_DRIFT = explicit "no studio gray", "no neutral gray product backdrop", "porcelain-white
            #FAFAFA dominant"
MAPS_TO_BRIEF_CHECK = L2 (via negation; no separate brief check for L11 — it is enforced through L2 + L7)
```

---

## 3. PRIOR-ATTEMPT USAGE RULES (for this LORA brief)

| Asset | Allowed reuse for LORA brief? | Why |
|---|---|---|
| Lyre V0.1 attempt_004 (flux/dev) — Empire monolith backdrop | NO | LORA backdrop = white void, not Empire monolith. Wrong spatial language. |
| Lyre V0.2 attempt_004 (flux/dev) — Empire monolith backdrop | NO | Same reason. |
| Lyre V0.2 attempt_006 (flux-pro) — material/color reference | INDIRECT ONLY | The MATERIAL quality of attempt_006 (proper matte ceramic) is the standard LORA's ring surface must match (LOCK_10). The IMAGE itself cannot be a positive reference — it has a body, which LOCK_01 forbids. Use as "we know flux-pro can render matte ceramic this clean" only. |

None of these may be used as IP-Adapter / img2img seed for the LORA brief. The LORA brief is a from-scratch prompt-only first attempt on flux-pro/v1.1.

---

## 4. WHAT THIS DOCUMENT DOES **NOT** DO

- ✗ Render
- ✗ Modify the brief MD or JSON
- ✗ Modify tool or config
- ✗ Modify `character_workflow/proposals/lora.json`
- ✗ Edit any canon file
- ✗ Establish `anchor_lora_001` (first render attempt does that, only if 12 checks pass)
- ✗ Asset-lock
- ✗ Promote any prior Lyre output as LORA positive reference
- ✗ Pre-determine first-render verdict (verification is a separate post-render Cowork task)
- ✗ Auto-draft a LORA Golden Patch brief (separate future task once Clean Digital Gold hex canon-locked)

---

## 5. NEXT_SAFE_TASK

```text
NEXT_SAFE_TASK = OPERATOR_REVIEW_LOCK_SET_THEN_PROCEED_WITH_BRIEF_RENDER:
  - Operator reads §2 (11 locks) and §3 (prior-attempt usage rules)
  - Operator confirms / revises / rejects the locks
  - Operator follows brief §12 NEXT_SAFE_TASK to:
      Step A: set operator_approval_token in LORA brief JSON
      Step B: ensure FAL_KEY exported
      Step C: dry-run (already verified by Cowork on 2026-05-31)
      Step D: real render (operator runs from Windows)
      Step E: Cowork drafts verification report against 12 L-checks
  - If first attempt PASSES all 12 checks → anchor_lora_001 established
  - If first attempt FAILS → operator picks: revise brief V0.2, swap model, ACCEPT_PARTIAL, etc.

DO NOT (regardless of outcome):
  - Treat any single render as canon-locking LORA's visual definition
  - Use the LORA render as positive reference for LYRA / Mikage / Lyre briefs
  - Auto-draft LORA Golden Patch brief without operator approval + Clean Digital Gold hex
  - Patch Canon V2 §7.0 substrate addition from this lock set
```

---

*End of LORA_REFACTOR_PRESENCE_CONTROL_REFERENCE_V0_1.md*
*11 locks recorded for the LORA Phase 1 Baseline brief. No render. No promotion. No anchor established. No canon edited. No asset locked.*
