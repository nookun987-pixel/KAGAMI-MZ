# MIKAGE_CONTENT_CADENCE_V0_1 — integration note (2026-07-03)

STATUS: **DRAFT — chưa lock** (đúng như footer của chính file gốc). File nguồn đã lưu tại
`docs/handoff/MIKAGE_CONTENT_CADENCE_V0_1.html`. File này chỉ ghi chú đối chiếu — KHÔNG tự gộp,
KHÔNG tự khoá bất kỳ mục nào bên dưới. Hai điểm cần operator ruling trước khi coi cadence này
là nguồn thật để đăng bài.

## Khớp tốt — không cần sửa gì

- 4 trụ nội dung (Build Log / Transmission / Signal / Motion) không mâu thuẫn brand voice hiện có
  (calm · mysterious · minimal, đã lock ở `AGENTS.md` / `design_system`).
- Arc "EP.01 It becomes one body → EP.05 THE AWAKENING" **khớp gần như 1:1** với các Stage trong
  `MIKAGE_LANE_A_ROADMAP.html` (Stage A done = EP.01 · Stage B deformation = EP.02 · Stage C
  locomotion = EP.03 · blade action = EP.04 · Stage E cinematic proof shot = EP.05). Đây là chỗ
  nối tốt — roadmap kỹ thuật và cadence public đang tự nhiên khớp nhau, đáng ghi lại làm neo.
- Bảng vai trò nền tảng (TikTok=engine khám phá, FB=retention) không đụng file nào đã có.

## 1 — Có thể chính là mảnh còn thiếu (không phải xung đột, mà là cơ hội)

Nhiều file trong repo (`MIKAGE_LORE_DRIP_SCHEDULE_V0_1.md`, `MIKAGE_PUBLIC_LORE_CADENCE_OVERLAY_V0_1_OUTLINE.md`,
`MIKAGE_NARRATIVE_EXECUTION_ROADMAP_V1.md`) đều nhắc tới một **"Growth Cadence"** đang chạy, coi
nó là nền tảng để overlay lên trên — nhưng **không file nào định nghĩa nó thật sự** (đều ghi
`CHUA_XAC_NHAN` cho thành phần chính xác). File cadence mới này (4 trụ + lịch tuần + giờ đăng) rất
có thể chính là bản định nghĩa "Growth Cadence" mà các file kia đang chờ. Nếu đúng vậy, nên nói rõ
để các file kia hết phải ghi CHUA_XAC_NHAN.

## 2 — CẦN OPERATOR CHỐT (xung đột thật, chưa tự sửa)

**a) Trùng slot Thứ 2.** `MIKAGE_LORE_DRIP_SCHEDULE_V0_1.md` (drip 1/tuần) và
`MIKAGE_PUBLIC_LORE_CADENCE_OVERLAY_V0_1_OUTLINE.md` đều khoá slot **Monday = lore-drip**
(`SLOT_DAY = Monday`, đã duyệt trước). Cadence mới cũng đặt **Thứ 2 = BUILD LOG**. Hai thứ khác
nhau cùng đòi Thứ 2. Cần anh quyết: dời 1 trong 2 sang ngày khác, hay coi build-log-Thứ-2 và
lore-drip-Thứ-2 là 2 tầng khác nhau (build log = short-form video, lore drip = text/caption riêng)
nên không thật sự đụng nhau?

**b) CTA "Follow for the drop" chưa có trong ngữ pháp CTA đã khoá.** Rule hiện hành
(`MIKAGE_TRANSMISSION_SYSTEM_V0_1_OUTLINE.md`, `MIKAGE_PUBLIC_LORE_CADENCE_OVERLAY_V0_1_OUTLINE.md §7`,
`design_system/SKILL.md`) chỉ cho đúng 4 dạng: `live` → **"Listen now"** · `future` → **"Pre-save"**
· `uncertain` → **"Link"** · unknown → **"Tagline — UNCONFIRMED"**. Cadence mới thêm **"Follow for
the drop"** làm lựa chọn cho track chưa live (dòng Guardrails + bảng 4 trụ). Đây là từ mới, chưa
từng được khoá. Cần anh chốt: thêm "Follow for the drop" vào ngữ pháp CTA chính thức (và cập nhật
các file rule ở trên), hay bỏ nó, chỉ dùng đúng "Pre-save" cho mọi track chưa live như luật cũ?

## Việc CHƯA làm (chờ operator trả lời 2a/2b ở trên)

- Chưa sửa `MIKAGE_LORE_DRIP_SCHEDULE_V0_1.md` / cadence overlay để né trùng Thứ 2.
- Chưa thêm "Follow for the drop" vào bất kỳ file CTA-lock nào.
- Chưa đổi tên EP.01–05 (cadence mới) để né trùng với `EPISODE_01`–`EPISODE_06` (thư mục build_log
  build_log đã có, nội dung khác hẳn: silhouette/world/design-code/off-the-page/(unconfirmed)/
  transmissions). Đề xuất: gọi series build-log fan-facing mới là **"STANDING HERO — EP.01…05"**
  thay vì bare "EP.01" để không lẫn với `EPISODE_01` đã tồn tại. Chờ anh gật.
