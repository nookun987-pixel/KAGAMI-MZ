# MIKAGE LOCATION SEEDS — V0.1 OUTLINE (Step 6 of Safe Plan)

Generated by: Claude Cowork (planning-only pass)
Generated on: 2026-05-29
Mode: OUTLINE ONLY. **No canon. No final lore. No map. No coordinates. No commit. No push. No render.**
Scope: **Step 6 of `MIKAGE_IP_WORLD_BACKGROUND_PLAN_V0_1_SAFE_REVISION.md` §4 ONLY.**
Status: pending GPT review.

**Labeling convention** (used throughout):
- `[CONFIRMED_SOURCE]` — directly supported by source context.
- `[CHUA_XAC_NHAN]` — unresolved / missing / not operator-approved.
- `[OUTLINE_ONLY]` — structural organization or symbolic-seed framing, not a canon claim.
- `[PROPOSAL_ONLY]` — every seed below; cannot exit this status without (a) Step 5 gate approval + (b) per-seed operator approval.

---

## 1. PURPOSE

`[OUTLINE_ONLY]`

This file is the **Step 6 Location Seeds outline** described in the Safe Plan §4. It introduces **5 symbolic, production-safe seed slots** as `PROPOSAL_ONLY` only. The seeds are not canon, not lore, not geography, not a map.

It is **not**:
- a list of places on a world map
- a coordinates table
- a faction structure
- a city / district / region
- a society or class system
- a public WORLD page
- a public-facing document
- a canon update or canon-lock
- a render or art task

It **is**:
- 5 symbolic production-shoot/context names
- each a `PROPOSAL_ONLY` planning anchor
- preparation for GPT review prior to operator approval

---

## 2. SOURCE FILES READ

`[OUTLINE_ONLY]`

| # | Path | Role |
|---|---|---|
| 1 | `docs/handoff/MIKAGE_IP_WORLD_BACKGROUND_PLAN_V0_1_SAFE_REVISION.md` | Step 6 scope + Step 5→6 patch-ordering rule |
| 2 | `docs/handoff/MIKAGE_TAB_WEB_READ_FIRST_IP_WORLD_CONTEXT_V0_1.md` | Binding facts + CHUA_XAC_NHAN + Forbidden Drift |
| 3 | `docs/handoff/MIKAGE_NARRATIVE_EXPANSION_GATE_V0_1_OUTLINE.md` | Gate protocol that every seed MUST pass through to exit `PROPOSAL_ONLY` |
| 4 | `docs/handoff/MIKAGE_WORLD_BACKGROUND_WITHOUT_GEOGRAPHY_V0_1_OUTLINE.md` | Step 2 background discipline (no geography) |
| 5 | `docs/handoff/MIKAGE_CHARACTER_SYSTEM_V0_1_OUTLINE.md` | Step 3 entity grid (no new entity); Zenith Blade + Tai Vane anchors |
| 6 | `docs/handoff/MIKAGE_TRANSMISSION_SYSTEM_V0_1_OUTLINE.md` | Step 4 transmission discipline (CTA grammar; INTERFACE palette) |

---

## 3. STEP 5 GATE CONFIRMATION

`[CONFIRMED_SOURCE]` (Safe Plan §7 patch-ordering rule + Step 5 outline §11):

```text
Step 6 (Location Seeds) CANNOT exit PROPOSAL_ONLY until:
  (a) Step 5 Narrative Expansion Gate outline is operator-approved AND committed
      to the repo, AND
  (b) Each seed below receives its own per-seed operator approval per the
      gate protocol §9 (OPERATOR_APPROVED = YES — SCOPE = <seed name>).
```

`[OUTLINE_ONLY]` enforcement note:
- This Step 6 outline is itself a `PROPOSAL_ONLY` planning document; writing it does NOT violate the patch-ordering rule because no seed below is promoted to canon. The seeds are sketched but **not approved**.
- Per Step 5 outline §9: approval is per-clause; no batch approval. Each of the 5 seeds is a separate clause requiring a separate `OPERATOR_APPROVED` line.
- The Step 5 outline itself is not yet operator-approved/committed at this writing. Even if Step 5 lands tomorrow, each seed below still needs its own approval.

---

## 4. LOCATION SEED RULE

`[CONFIRMED_SOURCE]` (Safe Plan §4 Step 6 + Step 5 outline §6 Hard-Exclude Filter):

Every seed below carries the locked banner from the Step 5 gate:

```text
STATUS = PROPOSAL_ONLY
ASSET_LOCK = NO
CANON_LOCK = NO
PUBLIC_REVEAL = NO
OPERATOR_APPROVAL = REQUIRED
```

`[OUTLINE_ONLY]` shared rules for all 5 seeds:
- **Symbolic, not geographic.** Each seed names a **production context** (where a shot / scene / surface can be staged in production work). It does NOT name a place on a Mikage-world map.
- **No coordinates.** No "north / south / east / west / district / sector / level".
- **No society.** No inhabitants, demographics, factions, or class system inside any seed.
- **No connection between seeds.** Seeds do NOT form a map, route, or adjacency graph.
- **No promotion.** A seed cannot be canon-locked, asset-locked, or public-revealed without the Step 5 gate + per-seed operator approval.
- **Two-Canon discipline.** Each seed declares its canon layer per instance. ART-only colors never enter INTERFACE-instance use; INTERFACE violet signal never enters ART-instance use.
- **Forbidden Drift filter inherited.** Read-first §10 + Step 5 outline §6 reject list applies in full.

---

## 5. SEED 01 — VOID STAGE

### Status banner

```text
SEED_ID = LOCATION_SEED_SLOT_01
NAME = Void Stage
STATUS = PROPOSAL_ONLY
ASSET_LOCK = NO
CANON_LOCK = NO
PUBLIC_REVEAL = NO
OPERATOR_APPROVAL = REQUIRED
```

### Function (production use this seed supports)

`[OUTLINE_ONLY]` — a **production-shoot context** built on the locked void-black canvas (Canon V2 §3.1 + alignment §C `VOID BLACK = silence, scale, distance`). Use case: a clean void backdrop for staging subject / blade / object compositions without any environmental clutter.

### Allowed use

`[OUTLINE_ONLY]`:
- Render context for ART-canon subject-and-prop compositions (Mikage portrait shots, blade close-ups, material studies).
- Reference framing for the 30-40-30 frame discipline (`[CONFIRMED_SOURCE]` Canon V2 §11 + IP-core §4.5).
- Backdrop for film-proof key visuals already in the inventory.

### Forbidden drift

`[CONFIRMED_SOURCE]` (inherited from Step 5 gate §6 + read-first §10):
- Must NOT become a "venue" with a name on a map.
- Must NOT acquire a "Void Stage location" placement, surrounding district, or owner.
- Must NOT be framed as Entropy City / Heights / Undercity / Platforms / faction territory.
- Must NOT receive any inhabitant, society, audience, or class structure.
- Must NOT appear on a public WORLD page (page #6 stays `DEFERRED`).
- Must NOT trigger public reveal (board stays `0 / 23`).

### Two-Canon layer note

`[CONFIRMED_SOURCE]` (alignment §B):
- Default layer: **ART canon** (render context, palette = porcelain + void + controlled crimson + kintsugi gold).
- INTERFACE use NOT applicable — the void canvas in INTERFACE is `#050508` for site/UI backgrounds, but that surface is already governed by the design system; Void Stage as a seed is for production shots, not site chrome.

### CHUA_XAC_NHAN items for this seed

- Exact dimensions / aspect-ratio bias — `CHUA_XAC_NHAN`.
- Operator-confirmed use case list — `CHUA_XAC_NHAN`.
- Any "Void Stage" appearance in public copy — `CHUA_XAC_NHAN` and rejected until Step 5 gate approves.

### Operator approval requirement

```text
To exit PROPOSAL_ONLY, operator must write verbatim:
  OPERATOR_APPROVED = YES — SCOPE = LOCATION_SEED_SLOT_01 "Void Stage"
Per gate §9. No batch. No implicit. No automated.
```

---

## 6. SEED 02 — SIGNAL CHAMBER

### Status banner

```text
SEED_ID = LOCATION_SEED_SLOT_02
NAME = Signal Chamber
STATUS = PROPOSAL_ONLY
ASSET_LOCK = NO
CANON_LOCK = NO
PUBLIC_REVEAL = NO
OPERATOR_APPROVAL = REQUIRED
```

### Function (production use this seed supports)

`[OUTLINE_ONLY]` — a **production context** built on the locked signal motif (Canon V2 §10 HUD + alignment §C `ELECTRIC VIOLET = correct/live signal` + design_system `signal_line.svg`). Use case: an enclosed-feeling context where transmission events can be staged (signal-line motif foreground, controlled emission, no characters required).

### Allowed use

`[OUTLINE_ONLY]`:
- Render context for transmission motif (signal-line + concentric core + amplitude ticks per design_system `signal_line.svg` `[CONFIRMED_SOURCE]`).
- Optional INTERFACE-canon variant: section-divider context on the MUSIC VISUAL page (alignment §F section 4).
- Backdrop framing for "current transmission" treatments on HOME or per-track release pages (alignment §F section 1 / 2).

### Forbidden drift

`[CONFIRMED_SOURCE]` (inherited):
- Must NOT become a "control room" with named operators, factions, or staff.
- Must NOT acquire a geographic placement.
- Must NOT appear in promotional copy describing it as a "place where Mikage transmits from".
- Must NOT cross-link with held-NOT_CANON terms.
- Must NOT introduce new lore about how transmissions originate.

### Two-Canon layer note

`[CONFIRMED_SOURCE]` (alignment §B):
- **Either** ART canon (render context) **or** INTERFACE canon (site/section motif). Each instance picks ONE; never both.
- ART instance: porcelain + void + controlled crimson + kintsugi gold. NEVER violet fill.
- INTERFACE instance: void + porcelain + violet signal only. NEVER crimson, NEVER kintsugi gold.

### CHUA_XAC_NHAN items for this seed

- Whether Signal Chamber is primarily ART or INTERFACE in operator's intent — `CHUA_XAC_NHAN`.
- Any link between Signal Chamber and a specific transmission/track — `CHUA_XAC_NHAN`.
- Any character presence inside Signal Chamber — `CHUA_XAC_NHAN`.

### Operator approval requirement

```text
OPERATOR_APPROVED = YES — SCOPE = LOCATION_SEED_SLOT_02 "Signal Chamber"
```

---

## 7. SEED 03 — PORCELAIN FIELD

### Status banner

```text
SEED_ID = LOCATION_SEED_SLOT_03
NAME = Porcelain Field
STATUS = PROPOSAL_ONLY
ASSET_LOCK = NO
CANON_LOCK = NO
PUBLIC_REVEAL = NO
OPERATOR_APPROVAL = REQUIRED
```

### Function (production use this seed supports)

`[OUTLINE_ONLY]` — a **production context** built on the locked porcelain material (Canon V2 §2.3 + §3.1 `Porcelain White #FAFAFA`). Use case: an open expanse where the porcelain material itself dominates the frame — subject-and-material studies, hero shots emphasizing the B4C shell aesthetic.

### Allowed use

`[OUTLINE_ONLY]`:
- Render context for Mikage hero shots (Reference Sheet V1 core assets staging).
- Material study context for B4C porcelain (`[CONFIRMED_SOURCE]` Canon V2 §2.3 specs: hardness 33 GPa, density 2.52 g/cm³).
- Backdrop for "shell rises from the void" canon-confirmed line from T05 PORCELAIN ASCENSION (`[CONFIRMED_SOURCE]` lore-drip patch §3 DRIP 6 + design_system data.js T05 line) — but only as compositional context; the tagline is the only T05-confirmed copy.

### Forbidden drift

`[CONFIRMED_SOURCE]` (inherited):
- Must NOT become a literal "field" (geographic plain, agricultural land, district).
- Must NOT have inhabitants, crops, weather patterns, or rural framing.
- Must NOT be cross-linked with Entropy City / Heights / Undercity / Platforms.
- Must NOT generate new T05 copy beyond the locked confirmed line.
- Must NOT serve as a place name in public copy.

### Two-Canon layer note

`[CONFIRMED_SOURCE]`:
- Default layer: **ART canon** (material + render context).
- INTERFACE use NOT applicable — porcelain on INTERFACE is the `#f2eeea` identity color for type/marks/UI, governed by the design system. Porcelain Field as a seed is for production shots, not site chrome.

### CHUA_XAC_NHAN items for this seed

- Exact lighting setup for Porcelain Field shots — `CHUA_XAC_NHAN`.
- Whether this seed is reserved for specific tracks (e.g., T05 PORCELAIN ASCENSION) — `CHUA_XAC_NHAN`.
- Operator-confirmed scope — `CHUA_XAC_NHAN`.

### Operator approval requirement

```text
OPERATOR_APPROVED = YES — SCOPE = LOCATION_SEED_SLOT_03 "Porcelain Field"
```

---

## 8. SEED 04 — BLADE AXIS

### Status banner

```text
SEED_ID = LOCATION_SEED_SLOT_04
NAME = Blade Axis
STATUS = PROPOSAL_ONLY
ASSET_LOCK = NO
CANON_LOCK = NO
PUBLIC_REVEAL = NO
OPERATOR_APPROVAL = REQUIRED
```

### Function (production use this seed supports)

`[OUTLINE_ONLY]` — a **production composition context** built on Zenith Blade discipline (Canon V2 §2.4 + Character System §11.1: `[CONFIRMED_SOURCE]` "absolutely straight (zero curvature)", "rectangular silhouette", "Appleseed geometry"). Use case: composition reference for blade-centric shots where the blade's vertical-rectangular axis organizes the frame.

### Allowed use

`[OUTLINE_ONLY]`:
- Composition reference for ART-canon blade-prop shots (Zenith Blade isolated study, blade-with-Mikage hero shots).
- Anchor for "rectangular silhouette" frame discipline (`[CONFIRMED_SOURCE]` Canon V2 §2.4 forbidden traits list: no curved katana / thin elegant blade / clean laser / fantasy ornaments).
- Reference for the `[CONFIRMED_SOURCE]` Canon V2 §11 item 7 micro-moment ("Side-channel feedback: Blade pulses red when spine compression detected") staging.

### Forbidden drift

`[CONFIRMED_SOURCE]` (inherited + Step 3 §11.1 anti-drift):
- Must NOT become a faction name ("Blade Axis faction" — explicitly forbidden as a faction beyond Three Ideologies).
- Must NOT become a place / coordinate / route.
- Must NOT extend to other weapons or other characters (Zenith Blade wielded by Mikage only; `wielder_dependency.never_wielded_by` enforced).
- Must NOT introduce new blade attributes beyond Canon V2 §2.4.
- Must NOT promote the blade's crimson core glow into "leakage" framing (rejected per alignment §0.6).

### Two-Canon layer note

`[CONFIRMED_SOURCE]`:
- Default layer: **ART canon** (composition + render context).
- INTERFACE use NOT applicable — blade imagery on a public site stays ART within an INTERFACE chrome (Step 4 §11 boundary rule); the seed itself is ART.

### CHUA_XAC_NHAN items for this seed

- Whether Blade Axis is a strictly vertical composition or allows horizontal variant — `CHUA_XAC_NHAN`.
- Whether Blade Axis pairs with specific Pose Tests (01 / 02 / 03) — `CHUA_XAC_NHAN`.
- Operator-confirmed scope — `CHUA_XAC_NHAN`.

### Operator approval requirement

```text
OPERATOR_APPROVED = YES — SCOPE = LOCATION_SEED_SLOT_04 "Blade Axis"
```

---

## 9. SEED 05 — ARCHIVE NODE

### Status banner

```text
SEED_ID = LOCATION_SEED_SLOT_05
NAME = Archive Node
STATUS = PROPOSAL_ONLY
ASSET_LOCK = NO
CANON_LOCK = NO
PUBLIC_REVEAL = NO
OPERATOR_APPROVAL = REQUIRED
```

### Function (production use this seed supports)

`[OUTLINE_ONLY]` — a **production context** for the archive concept that bridges (a) the alignment §D "Launch Arc" archive framing and (b) the Canon V2 §8.5 Tai Vane data-vault custodian role. Use case: a symbolic archive context for transmission-history surfaces (per-track release pages, archive views).

### Allowed use

`[OUTLINE_ONLY]`:
- INTERFACE-canon context for the TRANSMISSIONS section's Launch Arc archive view (`[CONFIRMED_SOURCE]` alignment §F section 2).
- Optional ART-canon backdrop for Tai Vane-adjacent visuals — **only as locked-attribute context** (Tai Vane = Archive Tower AI, data vault custodian); does NOT extend Tai Vane's 420m carbon-lattice concrete tower into a geographic placement (Step 3 §10 boundary preserved).
- Symbolic backdrop for "logged transmission" framing per DRIP 6 (`[CONFIRMED_SOURCE]` lore-drip patch §3 DRIP 6: "Every track is one transmission. ... The same signal, logged again and again.").

### Forbidden drift

`[CONFIRMED_SOURCE]` (inherited + Step 3 §10 + Step 4 §13):
- Must NOT extend Tai Vane's tower into a city / location placement / surrounding geography.
- Must NOT introduce new Tai Vane attributes beyond Canon V2 §8.5.
- Must NOT become a "data district" or "memory zone".
- Must NOT appear in public copy as a place name.
- Must NOT generate new public lore about how the archive works (lore-drip §6 framing is the locked external description).
- Must NOT trigger public WORLD page activation.

### Two-Canon layer note

`[CONFIRMED_SOURCE]`:
- **Either** ART canon (render context for archive-adjacent visuals) **or** INTERFACE canon (Launch Arc section UI treatment). Each instance picks ONE; never both.
- ART instance: porcelain + void + controlled crimson + kintsugi gold. NEVER violet fill. NEVER ARCHON neon palette.
- INTERFACE instance: void + porcelain + violet signal only. NEVER crimson/kintsugi gold/Z-Blue.

### CHUA_XAC_NHAN items for this seed

- Whether Archive Node is the same context as Tai Vane's tower or a separate symbolic context — `CHUA_XAC_NHAN`.
- Whether Archive Node has any operator-confirmed presentation on the live site — `CHUA_XAC_NHAN`.
- Operator-confirmed scope — `CHUA_XAC_NHAN`.

### Operator approval requirement

```text
OPERATOR_APPROVED = YES — SCOPE = LOCATION_SEED_SLOT_05 "Archive Node"
```

---

## 10. SHARED FORBIDDEN DRIFT

`[CONFIRMED_SOURCE]` — hard exclusion filter inherited from read-first §10 + IP-core §8 + Step 2 §9 + Step 3 §14 + Step 4 §13 + Step 5 §6 + §12. This Step 6 outline introduces ZERO new forbidden items and removes ZERO existing ones.

Step-6-specific application — applies to **every** seed above:

- No Entropy City / Heights / Undercity / Platforms-as-canon / megacity / class war / generic cyberpunk
- No factions beyond locked Three Ideologies (Empire / ARCHON-IX / Third Axis)
- No society, demographics, class structure, inhabitants
- No map, no coordinates, no adjacency graph between seeds
- No public WORLD page (#6 stays `DEFERRED`)
- No public reveal authorization (board stays `0 / 23`)
- No crimson leakage / entropy / failure framing
- No public ART-only color (crimson / kintsugi gold / Z-Blue) inside INTERFACE chrome
- No INTERFACE violet inside ART render
- No "out now" copy, no emoji, no hype language
- No invented tagline beyond T05 PORCELAIN ASCENSION
- No new entity, no new ideology, no new axis
- No humanoid avatar for LORA (Lock 3A — permanent)
- No silent promotion of any `CHUA_XAC_NHAN` item

If any downstream use of a seed reads as introducing one of the above, that use is rejected at the Step 5 gate before reaching the Step 6 seed.

---

## 11. CHUA_XAC_NHAN ITEMS

`[CONFIRMED_SOURCE]` — preserved verbatim from read-first §9 + IP-core §7 + Step 2 §8 + Step 3 §13 + Step 4 §12 + Step 5 §8. Step 6 promotes nothing. Re-listed with Step-6-specific additions:

Carried forward:
- Unbreakable Shield physicality (`WEAPON_DRIFT_001`)
- LORA "Root Architect" PUBLIC framing
- Location seeds (any kind — including the 5 seeds in this file)
- Human society structure
- City geography
- Factions beyond Three Ideologies
- WORLD page (#6)
- Z-Blue hex / use case
- Mikage production prompt seed
- Voice / dialogue profiles for all six entities
- Commander Lyre / LORA visual assets (`DOES_NOT_EXIST`)
- Clean Digital Gold hex
- Mikage / Lyre official height lock (both provisional)
- Track taglines beyond T05 PORCELAIN ASCENSION
- Dr. Aris physical appearance / age / gender / backstory
- Tai Vane tower placement / surrounding geography
- ARCHON-IX & LYRA-0 attributes beyond Canon §8.3
- Public approval status for any short hook / caption
- Release status of T30 本当の名前 (locked `uncertain`)
- Specific release dates beyond `data.js` entries
- Logo asset for public use / Wordmark final art
- 4 OPEN policy items from `SHORT_AUDIO_REPAIR_FINAL_LOCK_REPORT`

Added at Step 6 (per-seed):
- Each of the 5 seeds (Void Stage / Signal Chamber / Porcelain Field / Blade Axis / Archive Node) is itself `CHUA_XAC_NHAN` as `PROPOSAL_ONLY`.
- Per-seed operator-confirmed scope — `CHUA_XAC_NHAN` until each gets its individual approval.
- Per-seed canon-layer choice (ART vs INTERFACE for the two seeds that can sit in either) — `CHUA_XAC_NHAN` per instance.
- Any connection or relation between seeds — `CHUA_XAC_NHAN` (seeds are independent; no map).
- Any pairing of a seed with a specific track / character / object — `CHUA_XAC_NHAN`.

---

## 12. NEXT SAFE TASK

```text
GPT_REVIEW_STEP_6_LOCATION_SEEDS_OUTLINE_BEFORE_OPERATOR_COMMIT
```

After GPT review:
- If approved → operator decides whether to commit this Step 6 outline in an isolated micro-commit (suggested message: `docs: add Mikage location seeds V0.1 outline (Step 6, 5 PROPOSAL_ONLY slots)`).
- If revision requested → patch this file, do not rewrite.
- **Per-seed promotion still requires (a) Step 5 gate operator-approved AND committed AND (b) per-seed `OPERATOR_APPROVED = YES — SCOPE = <seed name>` per Step 5 gate §9.** Committing this Step 6 outline does NOT promote any seed; it merely commits the seed slots as `PROPOSAL_ONLY`.
- Step 7 (Public Lore Cadence Overlay) does NOT start until Step 6 is operator-approved and committed.

---

## WHAT CLAUDE COWORK DID NOT DO THIS PASS

- ✗ Create any canon location, place name, coordinate, or map
- ✗ Add any new faction, society, class, demographics, or inhabitants
- ✗ Activate / propose a WORLD page
- ✗ Authorize any seed for canon promotion (each is `PROPOSAL_ONLY`)
- ✗ Promote any `CHUA_XAC_NHAN` item
- ✗ Connect seeds into a map / route / adjacency graph
- ✗ Pair seeds with specific tracks / characters / objects (each pairing stays `CHUA_XAC_NHAN`)
- ✗ Extend Tai Vane's 420m tower into surrounding geography
- ✗ Modify canon V2 or any committed file
- ✗ Touch the Forbidden Drift list
- ✗ Render any art
- ✗ Create any public copy
- ✗ Commit anything
- ✗ Push anything
- ✗ Write more than one file
- ✗ Bypass the Step 5 gate (the seeds stay `PROPOSAL_ONLY` exactly because the gate is operator-approval-gated)

Only one operation: write this single Step 6 outline file at `docs/handoff/MIKAGE_LOCATION_SEEDS_V0_1_OUTLINE.md`.

---

```text
STATUS = STEP_6_LOCATION_SEEDS_OUTLINE_WRITTEN_FOR_GPT_REVIEW
NEXT_SAFE_TASK = GPT_REVIEW_STEP_6_LOCATION_SEEDS_OUTLINE_BEFORE_OPERATOR_COMMIT
```

*End of MIKAGE_LOCATION_SEEDS_V0_1_OUTLINE.md*
*Step 6 of 7. 5 symbolic seeds, all PROPOSAL_ONLY. No canon. No map. No coordinates. No commit. No push.*
*Per-seed promotion gated by Step 5 Narrative Expansion Gate § 9 approval requirement.*
