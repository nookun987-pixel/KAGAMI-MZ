# MIKAGE CAST — ONE-PASS BATCH RENDER MANIFEST — V1

Generated by: Claude Cowork (Mikage repo operator — Character Cast Lane)
Generated on: 2026-06-02
Status: **CONTROL DOC — RENDER-READY SET (operator runs the renders; Cowork does not render)**
Purpose: let the operator render the whole priority cast in one sitting, then hand back for one verification pass.

> Scope = the FAL render-gate (text-to-image) pipeline only. Every brief below is render-ready EXCEPT for shared
> operator pre-steps in §2 (do once for the whole batch). Nothing here is canon-locked, asset-locked, or
> production-ready. Cowork renders nothing — it prepared the briefs and will verify outputs afterward (no PASS
> without operator agreement).

---

## 1. BATCH SET (render these in one pass)

| # | Entity | Brief (.json the gate reads) | Era | Aspect | Compact constant in gate? | Token | Last real render |
|---|---|---|---|---|---|---|---|
| 1 | Commander Lyre (Phase 1) | `docs/automation/render_briefs/BRIEF_COMMANDER_LYRE_PHASE_1_IMPERIAL_DUTY_V0_3.json` | V2.5 | 2.35:1 (21:9) | **NO — add `COMPACT_PROMPT_V0_3`** | null → set | V0.2 attempt_006 = NOT_PASS |
| 2 | LORA (Refactor Presence, Phase 1) | `docs/automation/render_briefs/BRIEF_LORA_REFACTOR_PRESENCE_V0_2.json` | V2.5 | 2.76:1 | **YES — `compact_prompt_v0_2_lora_presence`** | null → set | V0.1 attempt_003 = FAIL (product render) |
| 3 | LYRA (Glitch Phantom, Phase 1) | `docs/automation/render_briefs/BRIEF_LYRA_GLITCH_PHANTOM_PHASE_1_V0_1.json` | V1/ARCHON | 2.35:1 (21:9) or 9:16 | **NO — add `COMPACT_PROMPT_V0_1_LYRA`** | null → set | none (first render) |
| 4 | Mikage Zenith + Zenith Blade (Phase 1 Imperial Clean) | `docs/automation/render_briefs/BRIEF_MIKAGE_ZENITH_WITH_BLADE_PHASE_1_V0_1.json` | V2.5 | 2.35:1 (21:9) | **NO — add `COMPACT_PROMPT_V0_1_MIKAGE_BLADE`** | null → set | n/a (canon ref exists; this is a new candidate) |

Note on #4 (Mikage): follows the LOCKED MIKAGE ZENITH V2.5 spec — **sealed Kitsune mask (no open eye slits)** and
**Phase 1 = Imperial Clean (no kintsugi/cracks/crimson; dark-red symmetric pattern)**; blade = closed B4C Compact-Idle
block. FAL gives a candidate; for full fidelity to the locked identity, escalate Mikage to the RunPod IP-Adapter
anchored pipeline (brief §8.2).

### Mikage full 3-phase set (synced entity↔weapon, per locked spec)
The protagonist now has all three synced phases as briefs (render P1 for the batch; P2/P3 when the damaged/overdrive
looks are wanted — each is a separate still):

| Phase | Brief | State |
|---|---|---|
| P1 Imperial Clean (batch #4) | `BRIEF_MIKAGE_ZENITH_WITH_BLADE_PHASE_1_V0_1.json` | sealed mask, sterile clean, blade closed B4C block dormant |
| P2 Fallen/Exile | `BRIEF_MIKAGE_ZENITH_WITH_BLADE_PHASE_2_FALLEN_EXILE_V0_1.json` | kintsugi cracks + crimson bleed, Ensō ember, blade shell split (Ti frame + dull-ember core) |
| P3 Execution | `BRIEF_MIKAGE_ZENITH_WITH_BLADE_PHASE_3_EXECUTION_V0_1.json` | scorch scars + blood-vessel crimson glow + Ensō red at nape + thermal mirage, blade full overdrive (floating Ti plates, max #E60000 core, acid vapor, Orbital-Logic UI) |

All three: sealed Kitsune mask (invariant), straight slab blade (never katana), token=null. P3 carries scoped
universal-lock exceptions (Orbital-Logic UI text + thermal/acid VFX) per its brief §6.1. RunPod recommended for P3
fidelity. Each is render-ready under the same shared §2 pre-steps (add a `COMPACT_PROMPT_V0_1_MIKAGE_P2` / `_P3`
constant or point the gate at the brief prompt).

All three are single-still, asset-pool, Phase-1 baseline. None is track-bound output. None establishes an anchor
until its post-render verification passes and the operator agrees.

---

## 2. SHARED PRE-RENDER STEPS (do ONCE for the whole batch)

These are the reasons the last two real renders failed (square / product framing). Fix once, then all three benefit.

1. **Aspect-ratio mapping (root cause).** Lyre attempt_006 and LORA attempt_003 came back square/product-framed
   despite W×H being set. Confirm `tools/render_gate/mikage_render_gate_fal.py` sends `fal-ai/flux-pro/v1.1` an
   `aspect_ratio` enum it honors (e.g. `21:9`, `9:16`, `16:9`) — not just raw width/height. Verify with a dry-run
   that `fal_request.json` carries the wide ratio. **This is operator/tool work; Cowork did not modify the tool.**
2. **Compact constants.** LORA (#2) already has its constant. For Lyre V0.3 (#1) and LYRA V0.1 (#3), either add
   `COMPACT_PROMPT_V0_3` / `COMPACT_PROMPT_V0_1_LYRA` (+ matching negatives) to the gate, or run the gate pointed at
   each brief's `prompt_pack.primary_prompt`. The compact text is in each brief's §4.
3. **Approval tokens.** Set `project_metadata.operator_approval_token` in each of the three JSONs before its render
   (the gate refuses to call FAL without it).
4. **FAL key.** Ensure `FAL_KEY` is exported in the shell.

---

## 3. RUN ORDER + PER-ITEM COMMAND

Recommended order: LORA first (its constant is already wired = fastest smoke test of the aspect fix), then Lyre,
then LYRA.

```text
# 0) one-time: fix aspect mapping in the gate, export FAL_KEY

# A) LORA (already wired) — dry-run then real
python tools/render_gate/mikage_render_gate_fal.py --brief docs/automation/render_briefs/BRIEF_LORA_REFACTOR_PRESENCE_V0_2.json --dry-run
python tools/render_gate/mikage_render_gate_fal.py --brief docs/automation/render_briefs/BRIEF_LORA_REFACTOR_PRESENCE_V0_2.json

# B) Commander Lyre V0.3 — after adding COMPACT_PROMPT_V0_3 (or pointing gate at primary_prompt)
python tools/render_gate/mikage_render_gate_fal.py --brief docs/automation/render_briefs/BRIEF_COMMANDER_LYRE_PHASE_1_IMPERIAL_DUTY_V0_3.json --dry-run
python tools/render_gate/mikage_render_gate_fal.py --brief docs/automation/render_briefs/BRIEF_COMMANDER_LYRE_PHASE_1_IMPERIAL_DUTY_V0_3.json

# C) LYRA V0.1 — after adding COMPACT_PROMPT_V0_1_LYRA (or pointing gate at primary_prompt)
python tools/render_gate/mikage_render_gate_fal.py --brief docs/automation/render_briefs/BRIEF_LYRA_GLITCH_PHANTOM_PHASE_1_V0_1.json --dry-run
python tools/render_gate/mikage_render_gate_fal.py --brief docs/automation/render_briefs/BRIEF_LYRA_GLITCH_PHANTOM_PHASE_1_V0_1.json

# D) Mikage + Zenith Blade V0.1 (Imperial Clean) — after adding COMPACT_PROMPT_V0_1_MIKAGE_BLADE (or pointing gate at primary_prompt)
#    NOTE: for full fidelity to the locked sealed-Kitsune identity, consider the RunPod IP-Adapter route instead (brief §8.2).
python tools/render_gate/mikage_render_gate_fal.py --brief docs/automation/render_briefs/BRIEF_MIKAGE_ZENITH_WITH_BLADE_PHASE_1_V0_1.json --dry-run
python tools/render_gate/mikage_render_gate_fal.py --brief docs/automation/render_briefs/BRIEF_MIKAGE_ZENITH_WITH_BLADE_PHASE_1_V0_1.json
```

Each real render writes to its own `runs/<BRIEF_ID>/attempt_NNN/output.png` (the gate does NOT auto-promote to
`output_path`; promotion happens only after verification).

COST = CHUA_XAC_NHAN (flux-pro/v1.1 per-image; operator's FAL account). Suggest 1 seed/brief for this pass, then
re-roll only the ones that look close.

---

## 4. AFTER RENDER (hand back to Cowork)

For each output, Cowork will draft a verification report against that brief's checklist (Lyre 14-check, LORA its
own, LYRA 11-check), exactly as done for Lyre attempt_006. Cowork will NOT mark PASS, establish any anchor, or
asset-lock — operator decides per entity. Anchors `anchor_lyre_001` / `anchor_lora_001` / `anchor_lyra_001` stay
CHUA_XAC_NHAN until their first passing render + operator agreement.

---

## 5. OUT OF THIS BATCH (intentionally not FAL-rendered here)

| Entity | Why not in batch | Where it lives |
|---|---|---|
| Mikage proportion-refine (body only) | Separate from the key-visual: the stocky full-body proportion fix is a RunPod pose-skeleton job, not a FAL still. (Mikage himself IS now in the batch as #4 — the key visual with the blade.) | `docs/handoff/MIKAGE_FULLBODY_PROPORTION_REFINE_SPEC_V1.md` |
| Zenith Blade (weapon) | Complete at reference level; structure canon LOCKED. | `docs/handoff/MIKAGE_ZENITH_BLADE_ACCEPTED_REFERENCE_CONTACT_SHEET_V1.md` |
| Unbreakable Shield (Lyre's) | Blocked: needs operator interpretation lock + a Lyre asset to show it in-use. | Cast ledger §3.5 — deferred |
| ARCHON-IX (faction) | Operator decision needed: character vs faction-only. No proposal JSON. | Cast ledger §3.6 — needs clarification |

---

## 6. GOVERNANCE

No render by Cowork. No canon-lock. No asset-lock. No production-ready/film/video/short/shotlist. Briefs are
PROPOSAL_NOT_CANON. The render-gate tool/config was not modified by Cowork. Git push = operator-side (sandbox
cannot reach the worktree).

---

## 7. REPORT BLOCK

```text
FILES_READ      = Lyre V0.3 + LORA V0.2 + LYRA V0.1 briefs ; LORA V0.2 attempt_001 report (dry-run) ; CAST_VISUAL_LEDGER_V0_1.md ; proposals lora.json/lyra.json
FILES_CHANGED   = 1 CREATED: docs/handoff/MIKAGE_CAST_BATCH_RENDER_MANIFEST_V1.md
VERIFY_STATUS   = render-ready set assembled (3 briefs); all token=null; aspect-mapping fix = shared pre-step (CHUA_XAC_NHAN)
ISSUES_FOUND    = gate aspect mapping must be fixed once before batch ; 2 of 3 need compact constants added
NEXT_SAFE_TASK  = OPERATOR runs §2 pre-steps → §3 batch → hand outputs back for Cowork verification
BLOCKERS        = operator tokens ; gate aspect/compact wiring ; FAL key + cost ; git push operator-side
```

---

---

## 8. RUNPOD EXECUTION KIT (recommended path — "rent pod and go")

The FAL gate (§1–§3) is one path. The preferred path is now the **RunPod ComfyUI kit** at `tools/cast_render_kit/`,
because local ComfyUI honors width/height (kills the square/product-framing bug) and enables IP-Adapter anchoring for
Mikage fidelity. It renders all 6 jobs (Lyre V0.3, LORA V0.2, LYRA V0.1, Mikage P1/P2/P3) in one pass.

| File | Role |
|---|---|
| `tools/cast_render_kit/cast_jobs.json` | 6 jobs: prompt + negative + checkpoint + WxH + steps/cfg/sampler + seeds (mirrors each brief) |
| `tools/cast_render_kit/render_cast_batch.py` | ComfyUI API batch renderer (stdlib only; optional `--ipadapter`) |
| `tools/cast_render_kit/RUNPOD_OPERATOR_RUNBOOK.md` | A→Z: rent → ComfyUI → models → render → collect → hand back |

Default stack: RealVisXL V5.0, ~1344×576 (LORA ~1536×560), 34 steps, 2 seeds/job. Operator runs; outputs are review
candidates; Cowork verifies after (no PASS/anchor/asset-lock without operator agreement). The FAL-gate token/compact-
constant steps in §2 are NOT needed for the RunPod path.

---

*End of batch render manifest. Cowork prepared; operator renders; Cowork verifies. No render/canon/asset-lock by Cowork.*
